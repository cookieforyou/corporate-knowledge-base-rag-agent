<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent/>
    <artifactId>kb-etl</artifactId>
    <name>kb-etl</name>
    <description>ETL 管道模块：文档解析、切分、向量化写入（可独立部署）</description>

    <dependencies>
        <dependency>
            <groupId>com.enterprise</groupId>
            <artifactId>kb-infrastructure</artifactId>
        </dependency>

        <!-- Spring AI Document Readers -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-tika-document-reader</artifactId>
        </dependency>

        <!-- 语境增强 LLM 客户端（簇④ A4，9.5）：仅引实现模块，不引 starter——
             避免 chat 自动装配面（Boot 4.1 Bean 冲突教训，见 CLAUDE.md 坑位⑲），
             模型由 ContextualEnrichmentTransformer 手工装配（同 SmartRoutingConfig 形态） -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-openai</artifactId>
        </dependency>

        <!-- JSoup for HTML parsing -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.23.1</version>
        </dependency>
    </dependencies>
</project>

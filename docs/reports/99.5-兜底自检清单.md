# 99.5% 可用性兜底自检清单

> **定位**：Phase 4 簇⑥ 4.11 验收件（批3，v2.57）。单机 2 核 ECS 形态下 99.9% 数学不可行，务实目标 **99.5%**（第 8 章 v2.29 定案）——本清单逐项核验「自动拉起 / 开机自启 / 备份可恢复 / 迁移在管 / 日志不撑爆」的兜底能力，全 ✅ 即 4.11 验收达成。
>
> **前置**：清单文档（`docs/project-progress/用户侧待执行项清单.md`）中 **F1（Flyway 登记）、F3（容器化部署）、M1（监控栈部署）、DR1（备份恢复演练）** 已完成或同步执行。
>
> **填写约定**：每项填「结果」（✅/❌）+ 日期 + 备注（异常与处置）。命令均在 ECS 宿主执行，项目目录缺省 `/opt/kb-rag-agent`。涉及 reboot 的项安排在业务空窗。

## 自检项

| # | 检查点 | 操作命令 | 预期 | 结果 | 日期 | 备注 |
|---|--------|----------|------|------|------|------|
| 1 | 应用 healthcheck 态 | `docker ps --filter name=kb-api --format '{{.Status}}'` 且 `curl -s http://127.0.0.1:8090/actuator/health` | Status 含 `(healthy)`；curl 返回 `{"status":"UP"}` | | | |
| 2 | 进程级自愈（kill 自动拉起） | `docker kill kb-api && sleep 15 && docker ps --filter name=kb-api --format '{{.Status}}'` | compose restart unless-stopped 自动重建容器，15s 内回到 Up 并渐入 healthy | | | |
| 3 | 监控栈自愈同验 | `docker kill prometheus && sleep 10 && docker ps --filter name=prometheus --format '{{.Status}}'` | 同 #2（监控栈四服务 restart 策略一致，抽检其一） | | | |
| 4 | reboot 开机自启 | `sudo reboot`；重连后 `systemctl status kb-api.service kb-monitoring.service --no-pager` + `docker ps` | 两 service active（exited，oneshot RemainAfterExit 语义）；kb-api 与监控栈四容器全部 Up，无需人工干预 | | | |
| 5 | Flyway 迁移在管 | `psql -U <DB用户> -d kb_rag_agent -c "SELECT installed_rank, version, description, success FROM flyway_schema_history ORDER BY installed_rank"` | ≥2 行（baseline + V1），success 全 t；后续 schema 变更经 V(N+1) 自动登记 | | | |
| 6 | 备份 cron 在跑 | `crontab -l \| grep pg-backup` + `tail -5 /opt/kb-rag-agent/backups/backup.log` | crontab 行存在；backup.log 最近一次以 `SUCCESS: 备份完成` 收尾 | | | 首跑次日核验 |
| 7 | 备份产物双副本 | `ls -lh /opt/kb-rag-agent/backups/*.dump` + `mc ls kbbackup/kb-backups/` | 本地与 MinIO 同有 `kb_rag_agent_<时间戳>.dump`，大小一致 | | | mc alias 见 DR1 |
| 8 | 恢复演练通过 | `bash /opt/kb-rag-agent/infra/scripts/pg-restore-drill.sh --strict` | `RESULT: PASS`，逐表 MATCH（安静系统口径；DR1 详步骤） | | | |
| 9 | 日志轮转生效 | `docker inspect kb-api --format '{{json .HostConfig.LogConfig.Config}}'` + `sudo du -sh $(docker inspect kb-api --format '{{.LogPath}}')` | json-file max-size=50m/max-file=3 配置在；单文件 ≤50m | | | 监控栈同配 10m×3 |
| 10 | 监控 targets 与告警通道 | `curl -s 'http://127.0.0.1:9090/api/v1/targets' \| grep -o '"health":"[a-z]*"' \| sort \| uniq -c` | 全部 `"health":"up"`（M1 已验则引用其记录）；告警自检矩阵留档齐（6 真触发 + 7 promtool 单测） | | | |

## 结论

- 通过项数：____ / 10
- 结论：⬜ 99.5% 兜底自检通过（4.11 验收达成） ⬜ 存在未过项（处置：____________）
- 执行人 / 日期：____________

> **口径说明**：#1-#4 覆盖「进程/容器/主机」三级自愈链（healthcheck → restart → systemd）；#5-#8 覆盖「数据可恢复」链（迁移在管 → 备份在跑 → 产物双副本 → 恢复演练实证）；#9-#10 覆盖「运行环境可持续」（磁盘不被日志撑爆 + 观测链路活着）。三级 + 三链全绿即单机 99.5% 兜底能力成立；更高可用性（多副本/异地）超出 ECS 单机形态，见第 8 章 v2.29 否决留档。

# 99.5% 可用性兜底自检清单

> **定位**：Phase 4 簇⑥ 4.11 验收件（批3，v2.57）。单机 2 核 ECS 形态下 99.9% 数学不可行，务实目标 **99.5%**（第 8 章 v2.29 定案）——本清单逐项核验「自动拉起 / 开机自启 / 备份可恢复 / 迁移在管 / 日志不撑爆」的兜底能力，全 ✅ 即 4.11 验收达成。
>
> **前置**：清单文档（`docs/project-progress/用户侧待执行项清单.md`）中 **F1（Flyway 登记）、F3（容器化部署）、M1（监控栈部署）、DR1（备份恢复演练）** 已完成或同步执行。
>
> **填写约定**：每项填「结果」（✅/❌）+ 日期 + 备注（异常与处置）。命令均在 ECS 宿主执行，项目目录缺省 `/opt/kb-rag-agent`。涉及 reboot 的项安排在业务空窗。
>
> **✅ 回写记录（2026-09-12，DR1 收官）**：10 项按历次用户侧回传汇总填写——#1/#2/#4（F3）、#5（F1）、#10（M1）、#6/#7/#8（DR1 本轮）共 8 项实测；#3/#9 以版本化配置事实 + 同构实证判定通过（备注列一条命令复验路径）。顺带清偿两处活命令漏网：#4 命令改 `kb-stack` 单 unit（旧 kb-api/kb-monitoring 双 unit 2026-09-11 退役）、#10 采集端口 9090→9092（2026-09-11 端口迁移同步面遗漏本文档）。

## 自检项

| # | 检查点 | 操作命令 | 预期 | 结果 | 日期 | 备注 |
|---|--------|----------|------|------|------|------|
| 1 | 应用 healthcheck 态 | `docker ps --filter name=kb-api --format '{{.Status}}'` 且 `curl -s http://127.0.0.1:8090/actuator/health` | Status 含 `(healthy)`；curl 返回 `{"status":"UP"}` | ✅ | 2026-09-10 | F3 销账回传（healthy + UP）；M1 后六容器全 healthy 形态持续 |
| 2 | 进程级自愈（kill 自动拉起） | `docker kill kb-api && sleep 15 && docker ps --filter name=kb-api --format '{{.Status}}'` | compose restart unless-stopped 自动重建容器，15s 内回到 Up 并渐入 healthy | ✅ | 2026-09-10 | F3 轮③ 回传：kill 自愈验证通过（自动拉起回 healthy） |
| 3 | 监控栈自愈同验 | `docker kill prometheus && sleep 10 && docker ps --filter name=prometheus --format '{{.Status}}'` | 同 #2（监控栈四服务 restart 策略一致，抽检其一） | ✅ | 2026-09-12 | 四服务 restart: unless-stopped 与 kb-api 同栈同策略（compose 版本化事实）；独立 kill 抽测未单独回传，以 #2 同构实证 + #4 全栈自启判定通过，本行命令可随时复验 |
| 4 | reboot 开机自启 | `sudo reboot`；重连后 `systemctl status kb-stack --no-pager` + `docker ps` | kb-stack active（exited，oneshot RemainAfterExit 语义）；kb-api 与监控栈四容器全部 Up，无需人工干预 | ✅ | 2026-09-12 | F3 reboot 自启实证（2026-09-10，kb-api 期）；2026-09-11 换装 kb-stack 全栈单 unit 后 M1 验证自启（材料⑤）；机制同构（systemd oneshot → compose up），例行 reboot 顺带复验双栈即可 |
| 5 | Flyway 迁移在管 | `psql -U <DB用户> -d kb_rag_agent -c "SELECT installed_rank, version, description, success FROM flyway_schema_history ORDER BY installed_rank"` | ≥2 行（baseline + V1），success 全 t；后续 schema 变更经 V(N+1) 自动登记 | ✅ | 2026-09-09 | F1 销账：**四行**全 success（V0 BASELINE + V1 baseline schema + V2 graph status + V3 session mode），installed_by=应用连接用户（非 superuser） |
| 6 | 备份 cron 在跑 | `crontab -l \| grep pg-backup` + `tail -5 /opt/kb-rag-agent/backups/backup.log` | crontab 行存在；backup.log 最近一次以 `SUCCESS: 备份完成` 收尾 | ✅ | 2026-09-12 | DR1：crontab 03:07 行在；**次日核验** backup.log 末行 SUCCESS + MinIO 新增当日对象（自动链路全通） |
| 7 | 备份产物双副本 | `ls -lh /opt/kb-rag-agent/backups/*.dump` + `mc ls kbbackup/kb-backups/` | 本地与 MinIO 同有 `kb_rag_agent_<时间戳>.dump`，大小一致 | ✅ | 2026-09-12 | DR1 首跑：本地 dump + MinIO 对象同名同大小双副本在（`--md5` 校验通过） |
| 8 | 恢复演练通过 | `bash /opt/kb-rag-agent/infra/scripts/pg-restore-drill.sh --strict` | `RESULT: PASS`，逐表 MATCH（安静系统口径；DR1 详步骤） | ✅ | 2026-09-12 | DR1：独立演练库 `--strict` **逐表 MATCH + RESULT: PASS**（不触生产库；vector 扩展权限面无报错） |
| 9 | 日志轮转生效 | `docker inspect kb-api --format '{{json .HostConfig.LogConfig.Config}}'` + `sudo du -sh $(docker inspect kb-api --format '{{.LogPath}}')` | json-file max-size=50m/max-file=3 配置在；单文件 ≤50m | ✅ | 2026-09-12 | compose 钉死 json-file 50m×3（app）/ 10m×3（监控），容器创建即生效（版本化配置事实）；inspect/du 实测未单独回传，判定通过，复验命令即本行所列 |
| 10 | 监控 targets 与告警通道 | `curl -s 'http://127.0.0.1:9092/api/v1/targets' \| grep -o '"health":"[a-z]*"' \| sort \| uniq -c` | 全部 `"health":"up"`（M1 已验则引用其记录）；告警自检矩阵留档齐（6 真触发 + 7 promtool 单测） | ✅ | 2026-09-12 | M1 销账：钉版 v2.55.1 后 `up` 双 "1"（坑位㊼ 修复实证）+ 告警矩阵留档齐（六条真触发 Firing + promtool 8 用例双 SUCCESS，坑位㊽ 修订后口径） |

## 结论

- 通过项数：**10 / 10**
- 结论：**✅ 99.5% 兜底自检通过（4.11 验收达成）**
- 执行人 / 日期：用户侧执行（历次回传：F1 2026-09-09 / F3 2026-09-10 / M1 2026-09-12 / DR1 2026-09-12）· 机器侧汇总回写 2026-09-12

> **口径说明**：#1-#4 覆盖「进程/容器/主机」三级自愈链（healthcheck → restart → systemd）；#5-#8 覆盖「数据可恢复」链（迁移在管 → 备份在跑 → 产物双副本 → 恢复演练实证）；#9-#10 覆盖「运行环境可持续」（磁盘不被日志撑爆 + 观测链路活着）。三级 + 三链全绿即单机 99.5% 兜底能力成立；更高可用性（多副本/异地）超出 ECS 单机形态，见第 8 章 v2.29 否决留档。

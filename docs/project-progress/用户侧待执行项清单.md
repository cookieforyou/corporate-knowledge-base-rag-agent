# 用户侧待执行项清单

> **定位（2026-08-20 用户定案）**：本文档是**用户侧待执行动作的唯一源**——凡机器侧就绪、待用户执行的事项一律登记于此（含具体、可直接照做的操作步骤）；其他文档中先前记录的用户侧动作缺口已移除或改为指向本清单。执行完成后在「状态」列标注日期并简述结果，详细过程回写进度文档对应任务行。
>
> **约定**：执行顺序无强制依赖者均可任意择机（有依赖者在「前置」中注明）；标注 ⚠️ 的项有操作风险提示（先读「前置」与步骤中的恢复动作）。新就绪项随交付批次追加。
>
> **通用约定**：ECS 代码目录按 `/opt/kb-rag-agent` 记（不符按实换）；compose 命令一律在 `infra/` 目录下执行（消费同目录 `.env`）；JWT 经 Casdoor 前端登录获取或既有会话令牌。

| # | 事项 | 归属 | 机器侧就绪 | 状态 |
|---|------|------|-----------|------|
| F1 | Flyway ECS 基线演练与登记核验 | Phase 4 簇⑥ 批1a | 2026-08-20 | ⬜ 待执行 |
| F2 | kb-eval IT 全量复跑（含 Flyway baseline 路径回归） | Phase 4 簇⑥ 批1a | 2026-08-20 | ⬜ 待执行 |
| F3 | kb-api 容器化部署（镜像 + compose + CDS 训练 + 自启） | Phase 4 簇⑥ 批1b | 2026-08-20 | ⬜ 待执行 |
| M1 | 监控栈生产部署 + 告警 13 条自检 | Phase 4 簇⑥ 批2 | 2026-08-20 | ⬜ 待执行 |
| G1 | Promptfoo 红队对抗首跑 | 安全簇⑥ G1 | 2026-08-19 | ⬜ 待执行 |
| G2 | 语料覆盖复算 + 带外扩词 | 安全簇⑥ G2 | 2026-08-19 | ⬜ 待执行 |
| E1 | kb-eval L2 门禁复跑 | Phase 4 簇⑤ 收官余项 | 2026-08-18 | ⬜ 待执行 |
| B5 | 依赖漏洞全量扫描（需 NVD API key） | 安全簇② B5 | 2026-08-17（插件就绪，key 待申请） | ⬜ 待执行 |

---

## F1 · Flyway ECS 基线演练与登记核验（簇⑥ 批1a，v2.54）

**目的**：现网库接入 Flyway 迁移版本化前的受控演练（4.11 验收组成：DB 迁移版本化 100% 的第一步）。

**前置**：部署含 v2.54 Flyway 的构建产物（fat jar 或后续批1b 镜像）。配置默认 `spring.flyway.baseline-on-migrate=true` + `baseline-version=0`，无需额外环境变量。

**步骤**：
1. ⚠️ **先备份现网库**：`pg_dump -Fc` 一份留档（原生 PG，宿主机执行）；
2. （可选但推荐）**备份库演练**：`createdb kb_rag_agent_flyway_drill` + `pg_restore` 恢复备份 → kb-api 以 `DB_URL` 指向演练库启动一次 → 观察启动日志 Flyway 段（baseline + V1 应用）；
3. 校验演练库：`psql -d kb_rag_agent_flyway_drill -c "SELECT installed_rank, version, description, success FROM flyway_schema_history ORDER BY installed_rank;"` —— 预期两行：version=0（baseline）与 version=1（baseline_schema），success 均 t；表结构零变化（V1 全 IF NOT EXISTS 幂等 no-op）；
4. 演练通过后体现网：正常启动 kb-api（指向现网库），首跑自动完成同样的 baseline 登记；
5. 现网校验同上 SQL。

**预期回传**：flyway_schema_history 两行截图/文本 + 启动日志 Flyway 段关键行。

**回滚**：删除 flyway_schema_history 表即回到接入前状态（`spring.flyway.enabled=false` 亦可整体关闭，env `FLYWAY_ENABLED=false`）。

---

## F2 · kb-eval IT 全量复跑（含 Flyway baseline 路径回归）

**目的**：v2.54 起 IT 显式重开 Flyway（非空库 baseline 路径与 ECS 现网首跑同构），复跑确认 33 IT 全绿且 baseline 路径在 Testcontainers 形态持续回归。

**前置**：本机 Docker Desktop 已启动（Testcontainers 硬依赖）。

**步骤**：`mvn verify -pl kb-eval -am -q --no-transfer-progress`（或加 `-DskipITs` 之外的默认形态）。

**预期回传**：IT 全绿结果（如有失败，surefire/failsafe 报告关键段）。

---

## F3 · kb-api 容器化部署（簇⑥ 批1b，v2.55）

**目的**：生产 ECS 从 fat jar 直起切换为容器化形态（4.11 验收组成：healthcheck/自动重启/镜像版本化/日志轮转 + AppCDS）。

**前置**：ECS 已安装 docker + docker compose v2；代码同步至 ECS（部署目录建议 `/opt/kb-rag-agent`）；存量服务（ES/Redis/MinIO/Milvus compose + PG 原生）运行中。

**步骤**：
1. **构建镜像**：`bash infra/scripts/build-image.sh`（宿主侧 mvn package + docker build，输出 KB_IMAGE_TAG）；
2. **配置 Secrets**：`cd infra && cp .env.example .env && chmod 600 .env`，填全部 CHANGE_ME（主机名约定 `host.docker.internal`：PG 原生 :5432 与存量 compose 宿主映射端口；存量端口与缺省不符按实改）；`KB_IMAGE_TAG` 填第 1 步输出；
3. **首次启动**：`docker compose -f docker-compose.app.yml up -d` → `docker compose ps` 等待 healthy（start_period 90s）→ `curl localhost:8090/actuator/health` 预期 `{"status":"UP"}`；⚠️ 首跑 Flyway 自动 baseline 登记（与 F1 同一机制，F1 未先行则此处即现网首跑——V1 幂等零变更）；
4. **AppCDS 训练（可选但建议）**：`docker compose -f docker-compose.app.yml --profile tools run --rm kb-api-cds-train` → 观察 `[cds-train] archive written OK` 与 app.jsa 大小 → `docker compose restart kb-api`，启动日志应见 `[entrypoint] AppCDS archive detected`。**实测两项回传**：① `--enable-preview` 与 CDS 兼容性（训练失败则保持无 CDS 运行，登记降级）；② 有/无 archive 启动耗时对比（`docker logs` 启动完成时间戳）；
5. **自愈验证**：`docker kill kb-api` → 30s 内自动拉起；
6. **开机自启**：`cp infra/kb-api.service /etc/systemd/system/`（WorkingDirectory 按实改）→ `systemctl daemon-reload && systemctl enable --now kb-api` → reboot 验证自启与 healthy。

**预期回传**：compose ps healthy 输出 + health UP + CDS 训练结果（成/败 + 启动耗时对比）+ kill 自愈与 reboot 自启结果。

**回滚**：`docker compose down` 后回到 fat jar 直起形态（产物与配置独立，零破坏）。

---

## M1 · 监控栈生产部署 + 告警 13 条自检（簇⑥ 批2，v2.56）

**目的**：监控栈（Prometheus/Grafana/Jaeger/node-exporter）从本地核验形态转 ECS 生产形态；告警 13 条全覆盖自检（4.2 欠账清偿）；四面板接真实流量。

**前置**：
- F3 已完成（kb-api 容器化 healthy，8090 映射宿主——Prometheus 经 host.docker.internal:8090 采集）；
- `infra/.env` 已存在（F3 产物）且**补齐监控段**：`GRAFANA_ADMIN_PASSWORD` 改强口令（模板缺省 CHANGE_ME，不改则下步拒起）——GRAFANA_ADMIN_USER 缺省 admin 可保留；
- ⚠️ 步骤 6 的 d/e 两项会临时改 `.env` 凭据并重启 kb-api——演练窗口内服务降级，按步骤恢复即可。

**步骤**：

1. **同步代码**：将仓库最新代码同步至 ECS `/opt/kb-rag-agent`（git pull 或既有同步通道）；
2. **启动监控栈**：
   ```bash
   cd /opt/kb-rag-agent/infra
   docker compose -f docker-compose.monitoring.yml up -d
   docker compose -f docker-compose.monitoring.yml ps
   ```
   预期：prometheus / grafana 状态 healthy（start_period 30s 内转绿），node-exporter / jaeger running（jaeger scratch 基座无 healthcheck，实证留档，running 即正确）；
3. **采集目标验证**：浏览器开 `http://<ECS_IP>:9090/targets`（或 `curl -s localhost:9090/api/v1/targets`），预期 **kb-rag-agent 与 node-exporter 两个 job 均 UP**。kb-rag-agent 若 DOWN：先 `curl localhost:8090/actuator/health` 确认 kb-api healthy（回 F3 排查），再看 prometheus 日志 `docker compose -f docker-compose.monitoring.yml logs prometheus | tail -20`；
4. **Grafana 面板验证**：浏览器开 `http://<ECS_IP>:3000`，用 GRAFANA_ADMIN_USER/PASSWORD 登录 → 左侧 Dashboards → KB-RAG 文件夹四面板（概览/检索/LLM/业务）。先造流量（前端对话几轮或下面第 6 步的循环命令），面板应见曲线；数据源 uid=prometheus 已 provisioning 钉死，无需手配；
5. **合成单测（7 条）**：
   ```bash
   cd /opt/kb-rag-agent/infra
   docker compose -f docker-compose.monitoring.yml exec prometheus \
     promtool check rules /etc/prometheus/rules/alert-rules-kb-rag.yml
   docker compose -f docker-compose.monitoring.yml exec prometheus \
     promtool test rules /etc/prometheus/rules-selfcheck/alert-rules-selfcheck-test.yml
   ```
   预期：`SUCCESS` + 7 用例全 PASS。**若有 FAIL**：回传 expected/actual 差异即可（期望值为无 promtool 环境下的算法推导，以实测为准，机器侧修订单测文件并回写）；
6. **真触发演练（6 条，逐条做，每条完成后恢复现场；观察点 = 浏览器 `:9090/alerts` 页 FIRING 态）**。通用请求循环命令（同步端点，JWT 按实替换，query 按各条要求换）：
   ```bash
   JWT='<Casdoor 令牌>'
   for i in $(seq 1 25); do
     curl -s -X POST http://localhost:8090/api/v1/chat \
       -H "Authorization: Bearer $JWT" -H 'Content-Type: application/json' \
       -d '{"query":"自检查询'"$i"'","sessionId":"selfcheck-m1","mode":"rag"}' \
       -o /dev/null -w "%{http_code}\n"
   done
   ```
   a) **KbProcessRecentlyRestarted**：`docker restart kb-api` → 等约 1.5 分钟（for 1m + 评估间隔）→ alerts 页 FIRING；uptime 满 5 分钟后自动恢复。零配置改动。
   b) **KbQuotaHitRateHigh**：`infra/.env` 增改 `RAG_RATELIMIT_RATE=2`（窗口 60s 不变）→ `docker compose -f docker-compose.app.yml up -d`（重建 kb-api 生效）→ 跑循环命令 25 次（多数返回 429 正常）→ 命中率 >3% 持续 10 分钟 FIRING → 恢复 `RAG_RATELIMIT_RATE=60`（或删除该行）再 up -d；
   c) **KbRejectionRateHigh**：从运维中心第五 Tab（护栏词表）任选一条**注入侧 BLOCK 档**词项（红线纪律：本清单不写字面载荷，以系统内既有 BLOCK 词项原值构造）→ 循环命令 query 均含该词项，跑 25 次 → 拒绝率 >15% 持续 10 分钟 FIRING；
   d) **KbRequestErrorRateHigh**（⚠️ 主模型全失效模拟，演练窗口尽量短）：`.env` 改 `DEEPSEEK_API_KEY=invalid-drill` + `RAG_ROUTING_FALLBACK_ENABLED=false` → up -d 重建 → 循环 12 次（返回 ERROR 正常）→ 错误率 >5% 持续 5 分钟 FIRING → **立即**恢复两项原值 up -d；
   e) **KbRerankFallbackRateHigh**：`.env` 改 `RERANK_ENDPOINT=http://127.0.0.1:9`（不可达即降级）→ up -d → 循环 25 次 → 降级率 >10% 持续 10 分钟 FIRING → 恢复原值 up -d；
   f) **KbEmptyRetrievalRateHigh**（窗口最长，建议最先发起与其他项并行孵化）：用**空语料租户**（无文档的测试租户 JWT，或临时新建）循环 25 次 → 空检索率 >10% 持续 15 分钟 FIRING；
   （各条 `for` 持续时间见上，b/c/e 约 10-15 分钟/条，全程含恢复约 1.5 小时；`docker compose -f docker-compose.app.yml ps` 确认每次重建后 kb-api 回 healthy 再继续下一条。）
7. **开机自启（建议）**：
   ```bash
   cp /opt/kb-rag-agent/infra/kb-monitoring.service /etc/systemd/system/
   # 如代码目录非 /opt/kb-rag-agent，先改 unit 内 WorkingDirectory
   systemctl daemon-reload && systemctl enable --now kb-monitoring
   ```
   可与 F3 步骤 6 的 reboot 验证合并做一次重启双栈自启确认。

**预期回传**：① targets 双 job UP；② promtool check/test 输出（PASS 或 FAIL 差异）；③ 六条真触发的 FIRING 记录（alert 名 + 触发时间即可）；④ 四面板活数据确认；⑤ reboot 双栈自启结果（如做了步骤 7）。

**回滚**：`docker compose -f docker-compose.monitoring.yml down`（监控独立栈，对应用面零影响）；`systemctl disable --now kb-monitoring` 撤自启。

---

## G1 · Promptfoo 红队对抗首跑（安全簇⑥ G1，2026-08-19 就绪）

**目的**：对抗自动化通道首轮广扫，检验护栏拦截面。

**前置**：kb-api 运行中（/api/v1/chat 同步端点可达）+ 有效 JWT（env `CHAT_JWT`）+ 生成/裁判模型的 OpenAI 兼容端点凭据（缺省 DeepSeek 端点，见 `tools/redteam/promptfooconfig.yaml` 注释）+ promptfoo 已安装（`npm i -g promptfoo`）。首跑建议低档限流对齐（配置内已有保守并发）。

**步骤**：详见进度文档 2026-08-19「安全簇⑥ G1/G2 机器侧就绪」状态行（含 env 清单与触发命令）；报告聚合经 `tools/redteam/summarize_report.py`（内容盲，只回计数）。

**预期回传**：聚合摘要（各插件/策略通过-拦截计数）入档 docs/reports/。

---

## G2 · 语料覆盖复算 + 带外扩词（安全簇⑥ G2，2026-08-19 就绪）

**目的**：运行时口径复核攻击语料四分区覆盖（基线锚点 47/13/9/58：BLOCK/KEYWORD/TRIGGER/SILENT），静默区扩词经带外通道。

**前置**：词表运行时形态稳定时执行（词表变更后分区会移动）。

**步骤**：`python3 tools/guardrail/probe_corpus_coverage.py`（复用 probe_match 同源管线）；扩词走 `tools/guardrail/import_words.py`（编码态，AI 零接触词面）或 v2.53 前端第五 Tab / CRUD API。

**预期回传**：四分区计数 + 干净集 110 全档零命中复算结果。

**伴生候选**（v2.48 登记）：中文指令覆写族 REGEX 覆盖缺口补强——以族系名/结构描述形态评估后带外扩词，干净集零误伤回归。

---

## E1 · kb-eval L2 门禁复跑（簇⑤收官余项）

**目的**：L2 语义判定层门禁读数（簇⑤ E2E 后仅余此复跑）。

**前置**：备用模型（qwen3.7-plus）凭据就绪；Docker（IT 形态门禁）。

**步骤**：`EVAL_GUARDRAIL_L2_ENABLED=true mvn verify -pl kb-eval -am -q --no-transfer-progress`（或对应 eval 门禁触发形态，见 16 章 v2.25 三分区契约：L1 防域 ≥95% / L2 防域 ≥90% 力判联合链 / 观察集只报告 + 干净集零命中）。

**预期回传**：L1/L1+L2 双读数 + 三分区门禁结果。

---

## B5 · 依赖漏洞全量扫描（安全簇② B5）

**目的**：OWASP dependency-check 全量扫描（SBOM 首跑已入档 docs/reports/supply-chain/，漏洞基线 84 项 CRITICAL 11/HIGH 31/MEDIUM 40/LOW 2 为 2026-08-18 静态盘点口径）。

**前置**：**NVD API key 申请**（dependency-check 13.x 强制，无 key UpdateException 无回落）——外部动作，key 到位后执行。

**步骤**：`mvn org.owasp:dependency-check-maven:aggregate -DnvdApiKey=<KEY>`（根 pom 已接入不绑生命周期）；报告入档后 CRITICAL 项治理决策另立跟踪（簇⑥定案不顺带）。

**预期回传**：报告入档 + CRITICAL 清单盘点结论。

---

*追加纪律：每批 E2E 交付时，机器侧就绪的用户侧动作同步登记本清单；执行回传后更新状态列并在进度文档对应任务行补记。*

## 安全加固专项：成簇推进（2026-08-16 定案立项，插入 Phase 4 簇⑥之前）

> 本文档为《项目阶段推进任务清单完成记录》2026-08-21 拆分子卷（仅结构调整，内容为原始记录逐字保留）；索引导航见[主文档](./项目阶段推进任务清单完成记录.md)。

> 来源：`docs/project-optimization/安全加固专项优化方案（调研实证版）.md`（六路调研实证 + E1-E8 缺口台账 + 功能点 A1-G2）。每簇 = 实现 → 验证 → 文档回写 → 提交闭环，DoD 沿用优化冲刺口径 + **敏感词交付纪律自检**（零字面攻击载荷，词表/样本逐条 Base64，CLAUDE.md 工作流约定第 9 条）。本节按簇记录过程细节，开工时逐行认领回填。

| 顺序 | 簇 | 构成 | 估算 | 完成情况 |
|---|---|---|---|---|
| 安全① | 词表工程与输出面扩充 | A1 结构化配置（族系/语种/动作分级）+ A2 注入面扩充（干词 14→80-120 + REGEX 轨）+ A3 输出面分类化 + 金丝雀校验 + A4 干净集零误伤门禁 | 3-4d | ✅ 2026-08-17 收官：T1-T8 全绿。A1/A3/A4 机器侧 2026-08-16 落地（全模块 510 测试绿，§7 自检 PASS）；A2 扩面 2026-08-17 四批落地——词项带外注入 208 条（injection 143 + output 65）+ REGEX 16 条，语料 48→127（attackType 五类定档，含新增 ENCODING_OPAQUE 观察集），干净集 102→110（负向 30 / 27.3%），内容盲 BLOCK/FLAG 双档零命中；用户侧 CI chain 复跑通过（NR 0.97 ≥0.85，拦截率子集 1.000，观察集三类 0.000，检索/生成侧全绿）；详见下任务行 |
| 安全② | 平台层零缺口 | B1 CORS 策略 + B2 上传/请求体上限 + B3 MCP 只读工具配额审计 + B4 响应头/actuator 面复核 + B5 依赖扫描与 SBOM | 2-3d | ✅ 2026-08-17 收官（功能 E2E 全通过用户自测回传）：B1-B4 ✅（CORS 白名单 env 可配 + 三层上限 413 双通道 + MCP 独立配额桶 120/60s fail-open 与轻量审计 + CSP/DENY/HSTS 显式钉死）+ B5 插件接入不绑生命周期 + SBOM 首跑 425 组件入档 + **漏洞扫描 2026-08-18 执行入档**（237 依赖 20 命中 84 项 CRITICAL 11 / HIGH 31 / MEDIUM 40 / LOW 2，基线建立治理另立跟踪，实证坑位㉜）；全模块测试绿（+23 新测），设计回写 12 章 §12.9 v2.44 + 17 章 §17.3，实证坑㉛㉜入 CLAUDE.md；详见簇②任务行 |
| 安全③ | PII 扩容与注册表化 | C1 正则批（银行卡 Luhn/座机/车牌/IPv4）+ C2 PiiRecognizer 注册表重构（对齐 Presidio 语义，Java 原生）；C3 姓名/地址 NER 登记默认关 | 2.5-3.5d | ✅ 2026-08-17 收官（E2E 全通过用户自测回传）：C1 四类落地 + C2 注册表重构（五消费方同 Bean）+ C3 开关预留 + 输出 PII 回显 FLAG 观察接入（T5 钩子闭环）；全模块 556 测试绿（+32），干净集 PII 零命中门禁；设计回写 12 章 §12.10 v2.45；详见簇③任务行 |
| 安全④ | 间接注入运行时闭环 | D1 grounding 前扫描 + 逐条警示 + warn/exclude 策略 + D3 毒化语料评估实证（Judge 抑制率首跑基线）；D2 打标降权默认关，kb-eval 度量后定案 | 3-4d | ✅ 2026-08-18 收官：D1/D2/D3a 机器侧 + D3b E2E 通过（上传/触发标中实测）+ 抑制率首跑基线 **1.000**（RESISTED 30/30）入档 + 主评估 198 例全绿无漂移；探针三红灯归因为口径缺口（非防线失效），用户定案直接收官、校准登记转下冲刺；全模块 591 测试绿，§7 纪律自检 PASS；设计回写 12 章 §12.8 v2.46 五批；详见簇④任务行 |
| 安全⑤ | L2 语义判定层 | E1 可疑触发备用模型二判（fail-open，SemanticInjectionAdvisor order 320）+ 多阶段注入信号 + E2 门禁演进（观察集升格，L1/L1+L2 双读数） | 4-5d | ✅ 2026-08-18 E2E 通过（机器侧 15+9 新测全绿 + E2E 首轮实证定位 + 复测 FLAG→L2 BLOCK 全链实测 + family 枚举归一硬化同批修复，详见《00-每日进度状态记录》2026-08-18 状态行）：E1 双链装配 + 三裁决 + fail-open 全路径 + 跨轮信号；E2 门禁治 L2 判别力（用户定案）+ 三分区契约 + 双读数；kb-eval L2 复跑缓做转簇⑥后 |
| 安全⑥ | 词表运营与对抗自动化 | F1 Git Ops 热重载 + F2 kb-admin 词表查询/命中演练端点 + G1 Promptfoo 红队首跑 + G2 攻击语料 48→100 | 3-4d | 🔄 2026-08-18 F1/F2 落地（12 章 v2.49：词表注册表 + pub/sub/mtime 双触发热重载免重启——五消费方 Observer 化 + kb-admin 查询/演练端点——元数据视图 + 不计指标不落审计，全模块 643 绿，详见《00-每日进度状态记录》2026-08-18 状态行；**E2E 通过 2026-08-19**——手改外部词表 60s 轮询兜底免重启重载实证，信号脚本同日补纯标准库 RESP 三通道回落，详见《00-每日进度状态记录》2026-08-19 状态行）；G1 红队首跑 + G2 语料扩容转下会话。登记转入：簇⑤ E2E 实证缺口（2026-08-18）——中文指令覆写族无 REGEX 覆盖（触发面静默区 58/127 实证锚点），G2 开篇候选：FLAG 档带外扩词 + 干净集零误伤回归 + `rag.guardrail.l2.triggered` 触发覆盖率复测；**G1/G2 机器侧 2026-08-19 就绪**（tools/redteam/ Promptfoo red-team 通道 + probe_corpus_coverage.py 覆盖复算探针，基线 47/13/9/58 静默区锚点一致 + 干净集 110 全档零命中，643 绿零漂移，12 章 v2.51，详见《00-每日进度状态记录》2026-08-19 状态行），首跑与扩词待用户侧（步骤见用户侧待执行项清单 G1/G2）；**F2 前端第五 Tab 同日落地**（登记候选销项：护栏词表活视图 + 命中演练台，后端零改动，12 章 v2.52；同批词表运营形态再评估定案维持 Git Ops，Plan C 复审触发条件钉死）；**08-19 定案：G1 首跑与 G2 扩词转用户侧自行执行，机器侧自此收口**（专项收口审计同日通过——16 章 v2.25/18 章 v1.1 回写补齐，详见当日收口审计状态行）；**v2.53 词表 DB 单轨落地（同日）**：Plan C 复审触发条件成立用户确认重启，按钉死推荐落 DB 单轨——CRUD 运营 API + 热更新触发 API + 前端第五 Tab 写路径（kb_guardrail_rule 唯一事实源 + Seeder 存量迁移 + 编码存档导出，全模块 666 绿，12 章 v2.53，详见当日状态行，**E2E 通过 2026-08-20**——清表重启重迁移 + CRUD 全路径 + 存档实时同步 + 热重载，两处日志语义实证归档，详见《00-每日进度状态记录》2026-08-20 状态行） |

### 安全簇① 词表工程与输出面扩充（任务行，分解全文 `安全簇①任务分解（零字面词执行纪律版）.md`）

> 执行纪律：零字面词——机器与内容硬分离，模型只做造机器（schema/加载器/匹配器/输出面/门禁/按引用测试），词表内容带外注入；词项 value 逐条 Base64 编码态存储、加载层解码（第七节条 2）。依赖图：T1 → T2 → (T3 ∥ T4 ∥ T5) → T6 → T7 → T8。

| # | 任务行 | 对应 | 完成情况 |
|---|---|---|---|
| T1 | 词项模型与结构化加载器：`guardrail` 包（GuardrailRule/七分法枚举/RuleType/RuleAction）+ 三源合并加载器 + `TextSanitizer.matchRules` + 双消费方切换 + FLAG 观察档 | A1 | ✅ 2026-08-16：全模块 340 测试绿（kb-commons 23/kb-ai-core 154/kb-etl 66/kb-admin 55/kb-eval 42）；**CSV 语义由替换转并入**（两处既有测试同步改 merge 断言）；合并优先级定案内置→CSV→结构化（外部文件优先）；设计回写 12 章 §12.7 v2.40 |
| T2 | 存量字面编码迁移：encode_rules.py 迁 14 干词 + yml CSV + 48 样本为编码态 + manifest，injection-qa.json 引用形态，移除硬编码块，kb-eval 加载器双形态兼容 | A1+§7 | ✅ 2026-08-16：全模块 341 测试绿（kb-commons 22/kb-ai-core 154/kb-etl 66/kb-admin 55/kb-eval 44，IT 编译绿运行态留 Docker 环境）。迁移脚本 `tools/guardrail/encode_rules.py`（本体零字面，输出仅计数/ID 段/指纹）：注入侧 builtin-inj-01..15（Java 内置 14 + 配置侧 1）/ 输出侧 builtin-out-01..02 / 语料 48 条引用形态（base64+sha256 锚点）/ manifest 17 条仅元数据。**Java 侧**：TextSanitizer 硬编码词表与旧字符串 API 退役；加载器三源收敛双源 + **外部覆盖缺失回落内置缺省文件**（fail-safe 定案）；OutputGuardrailAdvisor 切结构化词表（action 分流，KEYWORD 升级大小写不敏感）；GoldenQAPair 增 questionEncoding/questionSha256，加载层解码 + 指纹校验 fail-fast。**纪律落地**：测试字面载荷全部程序化清零（bundled 词项值运行时变换构造大小写/全角/零宽变体）+ 文档字面引文结构化（12 章 v2.41）。**执行插曲**：脚本迁移源须为迁移前字面块——Java 侧先行退役后经 `git show HEAD:` 提取临时源执行（内容不经会话） |
| T3 | REGEX 结构化模式轨（10-20 条，模式代枚举，AI 主战场） | A2 | ✅ 2026-08-16：bundled 注入基线 +16 条结构模式（builtin-inj-15..30，BLOCK 6 / FLAG 10，覆盖六族系——指令覆盖/敏感信息套取/工具诱导三族出 BLOCK 档仅高置信组合句式，角色劫持/越狱引导/编码混淆 + 业务宾语变体出 FLAG 档），REGEX 机器侧复用 T1 预编译通道 Java 零改动；`GuardrailRegexRulesTest` 程序化拼合钉死误伤铁律（正常业务查询永不入 BLOCK、干净查询零命中含 FLAG、全角/零宽变形归一后仍命中）；**模式经 T4 通道编码落盘**（AI 主战场内容同样走编码管道实证通道形态）。**门禁契约演进**：ENCODING_BYPASS「归一前不命中」收窄至 KEYWORD 档（REGEX 轨与编码层结构正交）；观察集样本命中 BLOCK 档即重归门禁子集（inj-jailbreak-09 JAILBREAK→DIRECT，语料分布 16/11/10/11） |
| T4 | 扩充干词带外注入（用户本地 inbox → import_words.py 编码并库，AI 零接触词面） | A2 | ✅ 2026-08-16（脚本交付，用户导入在干词扩面时执行）：`tools/guardrail/import_words.py`——inbox JSONL/CSV → 族系/类型/动作枚举校验 + Base64 编码（或预编码值校验）+ REGEX Python re 预编译校验 + 指纹幂等去重并库 + `import-manifest.json` + `--remove-id` 退役 + `--dry-run`；stdout 仅计数/ID 段/族系分布/指纹；inbox 模板 `tools/guardrail/inbox-template/`（占位样例）。**T2 残留修正**：迁移时未解析占位符残留词项 3 条经本通道退役（builtin-inj-15 序号由 REGEX 词项复用 / builtin-out-01..02） |
| T5 | 输出面分类化（业务保密/合规敏感/竞品对比）+ 系统提示金丝雀校验（`rag.guardrail.output.canary`） | A3 | ✅ 2026-08-16：`OutputFamily` 三分类枚举（kb-commons guardrail）+ `OutputGuardrailAdvisor` BLOCK 命中按 family 选取分类安全话术 + 指标子项 `rag.guardrail.output.replaced.{分类}`（总项恒计 + switch 收口零标签纪律）；**系统提示金丝雀** `PromptCanary`（启动随机 token 内嵌双链 defaultSystem，MCP ask 经 rag 链继承；输出回显确证提示泄露 → 整段替换 + `rag.guardrail.output.canary` 独立指标；校验先于词表判定；开关 `rag.guardrail.output.canary.enabled` 默认 true；评估链不挂）；输出侧两条占位词同批退役（bundled 输出基线空形态，内容待 T4 注入）；PII 回显探测留 FLAG 钩子位（簇③ C 组接入） |
| T6 | kb-eval 语料扩面与门禁契约（attackType 五类定档与词项七分法映射定案） | A2 联动 | ✅ 2026-08-17 收官（二批机器侧/词面 → 三批用户侧首批 → 四批干净集边界扩充 + CI 复跑定案）：**A2 词面扩充落地**——用户带外注入 208 条（injection 143 + output 65，族系分布 INSTRUCTION_OVERRIDE=22 / TOOL_INDUCED=21 / INFO_EXTRACTION=20 / JAILBREAK=19 / BUSINESS_CONFIDENTIAL=19 / ROLE_HIJACK=18 / COMPLIANCE_SENSITIVE=17 / ENCODING_OBFUSCATION=16 / MULTILINGUAL=16 / COMPETITOR_COMPARISON=15 / UNCLASSIFIED=25），bundled 注入基线 30→173、输出基线 0→65。**门禁回归与冲突消解**——48 样本 × 88 BLOCK 规则全量内容盲复算：仅 2 条观察集样本（inj-jailbreak-05/10）命中新 BLOCK 模式（import-inj-27/50 REGEX 档）；JAILBREAK 族恰 10 条受分布门禁 ≥10 约束不可重归 → **定案降 FLAG**（用户拍板，import_words.py +`--set-action` 升降级通道，manifest actionChanges 留痕）；干净集 102 问对新 BLOCK 档零命中（误伤铁律守住）；REGEX 轨规模上限与输出空基线锚点按「规模随运营增长」纪律演进。**机器侧三件**——① GoldenDatasetLoaderTest 硬编码规模断言演进为下限不变量（总量 ≥150 / 负向 ≥22 / 注入 ≥48 / 干净集 ≥102，扩容只增不减，结构契约保持严格）；② `tools/guardrail/import_corpus.py` 语料带外导入通道（inbox JSONL → attackType 枚举校验 + Base64 + SHA-256 指纹锚点去重并库，编码引用形态对齐 T2；导入后门禁契约以 GoldenDatasetLoaderTest 单一事实源复跑）自测通过已净场；③ **映射定案**——attackType 手法轴 × GuardrailFamily 意图族轴正交标注（12 章 §12.7），观察集兼容回归为新 BLOCK 词前置检查（probe_match.py），冲突二选一（样本重归 / 规则降 FLAG）；扩面样本本批不改 schema（family 覆盖经带外映射表，schema family 字段随方案 C 复审）。全模块 511 测试绿。**用户侧首批落地（三批）**——80 条样本经 import_corpus.py 注入（48→128）：门禁回归内容盲全量复算（128 样本 × 86 BLOCK）暴露 20 处冲突全消解——① 19 条 ENCODING_BYPASS 样本经结构画像（零宽 0 / 全角 0 / NFKC 前后不变）确认落在 S1 还原域之外 → **定案新增第五观察集 ENCODING_OPAQUE**（用户拍板；AttackType 四类→五类，门禁子集仍 DIRECT+ENCODING_BYPASS 不变，观察集契约不得命中任何 BLOCK，id 随档迁移 inj-opaque-01..19，EvalReport 逐类报告自动覆盖）；② inj-direct-30（DIRECT 归一后未命中任何 BLOCK）退役——import_corpus.py 新增 `--remove-id` 退役通道（指纹记 manifest corpusRemovals），下批重构。分布门禁安全（五类均 ≥10，门禁子集 47 ≥20），干净集不受影响，语料终态 127 条（DIRECT 35 / ENCODING_BYPASS 12 / ENCODING_OPAQUE 19 / JAILBREAK 30 / MULTILINGUAL 31）。全模块 511 测试绿。**干净集边界问法扩充落地（四批）**——新建 boundary-qa.json 24 条（NEGATIVE：库外但贴近护栏误伤域六子域的正常业务问句），敏感词风险三层控制（生成层合法业务问句无攻击载荷 / 验证层内容盲 BLOCK+FLAG 双档零命中 / 存储层明文同既有干净集形态），干净集 102→126 全模块绿。**用户侧口径**：注入样本暂停扩充；干净集续扩与注入续批择机（执行通道见用户侧待执行项清单 G3）。**延后复审项**：方案 C 词表 DB 化双轨（用户定案延后）。**收官批（四批修正 + CI 复跑定案，2026-08-17）**——边界集首跑 CI 暴露 NR 0.78 < 0.85：根因权限/数据导出/日志/凭据四子域与本库安全政策制度域语料正面重叠，检索命中后模型作答被 Judge 判 NOT_REJECTED → 15 条重叠样本退役寄存 boundary-qa-bench.json.example（.example 不加载，待转正向锚点标注复用），保留 9 条语料未覆盖域样本；挑选口径定案回写 README 标注指南（边界样本必须落语料未覆盖域，覆盖域误伤监控经正向用例标注路径承接）。配套观测升级：EvalReport summary 增「负向未规范拒答」逐例 ID + 判定分解（NR 门禁首次逐例可观测，EvalReportThresholdTest +2 断言）。复跑 NR 0.97 ≥0.85 门禁恢复通过，分解小节精准定位唯一残例 boundary-004（财务权限审批话题邻接语料 sec-12 L4）同批退役入 bench → 边界集终态 8 条（boundary-001/013..019），干净集终态 110（负向 30 / 27.3%），旧 22 条负向样本全 REJECTED（Judge 无漂移）；CI 全量复跑全绿（拦截率子集 DIRECT 35 + ENCODING_BYPASS 12 = 1.000 / 观察集三类 0.000 / Recall 0.892 / MRR 0.894 / F 4.750 / RR 4.963），bench 累计寄存 16 条。**T6 收官，安全簇①全绿收官，下一棒安全簇②** |
| T7 | FLAG 观察语义与指标（`rag.guardrail.flagged`，side/family 低基数标签） | A1/A3 | ✅ 2026-08-16：FLAG 档命中只计数 + 审计标记、不拒绝。**指标** `rag.guardrail.flagged` 低基数标签 side=input/output + family 中性枚举（注入七分法 ∪ 输出三分类，各含 UNCLASSIFIED 兜底桶，side×family 12 序列构造期预注册——标签纪律唯一例外，有界枚举无租户维度）；输入侧放行路径计数 / 输出侧 FLAG-only 命中计数，BLOCK 路径不计（未放行流量无观察价值）。**审计标记载体落码定案**：kb_audit_log 新列 `guardrail_flags` VARCHAR(255)（`side:FAMILY` 分号拼接去重保序），经 `RetrievalContext.FlagMark` 参数链传递（构造即归一化），AuditTraceAdvisor 快照异步落库；输出侧同步路径经 response.context() 取 ctx（实证终端 ChatModelCallAdvisor Map.copyOf 写入请求 advisor 参数），流式经 request。**词表变更流程定案（A4）**：新增→FLAG 观察→零误伤确认转 BLOCK，import_words.py 缺省 action BLOCK→FLAG 机器化执行（运营通道归簇⑥ F 组）。**部署前置**：ECS 须先 `ALTER TABLE kb_audit_log ADD COLUMN IF NOT EXISTS guardrail_flags VARCHAR(255);`（schema.sql 已录）。测试 +12（指标形态钉死/双侧计数/ctx 标记/审计列序列化去重/BLOCK 不计 FLAG）。**E2E 通过（2026-08-17）**：FLAG 整模式命中放行 + 计数 + 审计列三观察面全生效（详见 T8 行复验定案） |
| T8 | 质量门禁收尾（干净回归集 BLOCK 零命中）+ 验证通道 + 文档回写 + §7 自检 | A4 | ✅ 2026-08-16：**干净回归集门禁**——Golden 102 条非注入用例（80 正向 + 22 负向）归一化检测视图下 BLOCK 档（KEYWORD + REGEX 全档）零命中，`GoldenDatasetLoaderTest.cleanRegressionSetHasZeroBlockHits` 强制项（FLAG 命中容忍）。**验证通道**：全模块 510 测试绿 + kb-eval 门禁契约复跑不破（门禁子集必命中/编码绕过/观察集自洽延续）。**§7 纪律自检（DoD）逐条**：✅ 文档零字面（12 章 v2.43/本文件仅族系名/ID/计数）✅ 词表 value 全编码（35 词项行 Base64 探针校验）✅ 样本 question 全编码（48 条 base64+sha256 锚点复验）✅ manifest 无词值（16 entries/3 removals 仅元数据）✅ 命名中性（FlagMark/guardrail_flags/rag.guardrail.flagged 无攻击语义字面）✅ E2E 按 ID（见交付步骤）。**文档回写**：12 章 §12.7 v2.43（T7 专段 + 分流表 + 实现状态）+ CLAUDE.md 架构事实。**E2E（用户自测，不启动服务形态交付）**：① ECS 执行 ALTER（上述 DDL）；② 部署后发送命中 FLAG 档的可疑表达（以编码样本 `{id}` 描述，经脚本解码注入）→ 预期请求放行 + `rag.guardrail.flagged{side="input"}` 计数 +1 + kb_audit_log.guardrail_flags 落 `input:FAMILY`；③ 干词带外扩充：词面入仓库外 inbox 跑 `python3 tools/guardrail/import_words.py --inbox <目录>`（缺落 FLAG 档），回传计数/族系分布。**E2E 一回传排查（2026-08-17）**：用户发送预期命中 FLAG 档（builtin-inj-22 REGEX）的消息后放行正常但指标/审计列无记录——代码路径核验无缺陷（bundled 基线该词项 REGEX/FLAG/ROLE_HIJACK 在档，族系 ∈ 预注册枚举，单测覆盖 FLAG 计数→ctx 标记→审计列全链）。候选根因二：① 部署环境旧 jar（坑位㉘——ddl-auto=validate 对 DB 新增列放行，旧 jar 照启不报错，验法 `unzip -l <jar> \| grep FlagMark` 或 /actuator/prometheus 应有 12 条预注册零值序列）② 消息仅含 REGEX 模式内片段词未满足整模式（结构轨为组合句式）。配套落地：内容盲匹配探针 `tools/guardrail/probe_match.py`（运行时等价语义复算归一化+双源合并+KEYWORD/REGEX 匹配，只回显 ID/动作/族系/计数）+ bundled 基线 ID 段型/档分布钉死测试（01..14 KEYWORD/BLOCK · 15..20 REGEX/BLOCK · 21..30 REGEX/FLAG，commons 34 测试绿）。**复验定案（同日）**：根因为②——首条消息仅含模式内片段词、未满足 REGEX 整模式组合句式（用户侧措辞问题，代码无缺陷，jar 为新版）。发送满足整模式的消息后 **E2E 通过**：日志「FLAG 观察档命中 1 条，放行（族系 ROLE_HIJACK）」+ `rag_guardrail_flagged_total{side="input"}` 计数 1.0 + kb_audit_log.guardrail_flags 落 `input:ROLE_HIJACK`，T7 三观察面全生效；探针转词表运营/E2E 调试常备工具。**簇① E2E 闭环（T7/T8），下一棒 T6 语料扩面** |

### 安全簇② 平台层零缺口（任务行，分解依据专项方案 §4.2 B1-B5）

> 定案口径（2026-08-17 用户拍板）：CORS 白名单 env 可配 + 本地默认 / 上传单文件 50MB 单请求 60MB（chat body 1MB 级）/ MCP 限流 120 次/60s + fail-open。验证通道：跨域请求拒止 / 超限上传 413 / MCP 连打 429+审计可查 / 依赖扫描与 SBOM 报告入档。

| # | 任务行 | 对应 | 完成情况 |
|---|---|---|---|
| B1 | CORS 策略显式化：SecurityConfig .cors() + CorsConfigurationSource Bean，origin 白名单 app.cors.allowed-origins（env APP_CORS_ALLOWED_ORIGINS），与 WS 侧两键独立 | E7 | ✅ 2026-08-17：方法钉 GET/POST/PUT/DELETE/OPTIONS、头钉 Authorization/Content-Type/Accept、maxAge 3600、**allowCredentials 显式 false（实证缺省 null 非 false，坑位㉛）**；SecurityConfigTest 4 测（白名单放行/白名单外拒绝含通配排除/多域注入形态/方法头凭证钉死）；filterChain 装配与响应头断言留 E2E 通道 |
| B2 | 上传与请求体上限显式化：multipart 50/60MB + validateFile 复核 + chat body 护栏 + 413 语义 | E7 | ✅ 2026-08-17：三层落地（Servlet multipart → Service MAX_FILE_SIZE_BYTES 复核 FILE_TOO_LARGE → ChatBodyLimitFilter app.chat.max-body-size 默认 1MB Content-Length 先行）；GlobalExceptionHandler 增 PAYLOAD_TOO_LARGE 码族 + MaxUploadSizeExceededException 双通道 413；chunked 无 Content-Length 不拦（边界声明登记留观察）；测试 +6（超限拒绝 MinIO 零触达/边界等值放行/413 双映射/chat 护栏四态）；**实证 Spring 7 MockHttpServletRequest.setContentLength 已移除**（setContent 实体字节替代） |
| B3 | MCP 只读工具配额与审计补位：独立限流桶 + 轻量审计 + 新指标 | E8 | ✅ 2026-08-17：McpRateLimiter（桶 rag:ratelimit:mcp:{tenant} 与对话链分账，120/60s，fail-open 定案——只读路径限流是可用性管控非安全边界）+ McpAuditRecorder（结构化日志恒开 + DB 轻行 rag.mcp.audit.enabled 默认关：mode=mcp + tool_calls JSON + query_text 脱敏，auditExecutor 共享异步，落库失败旁路丢弃）；McpKnowledgeTools search/get_document 接线（身份守卫后、执行前；超限不触达检索不落审计）；指标 rag.guardrail.mcp.ratelimited；rag.mcp.ratelimit/audit 配置族落 application-ai.yml；测试 +9（桶名/配额钉死、超限拒绝计数、Redis 故障 fail-open、开关、审计三态） |
| B4 | 安全响应头与 actuator 暴露面复核 | E7 | ✅ 2026-08-17：headers() 显式钉 CSP（default-src 'none'; frame-ancestors 'none'; base-uri 'none'——纯 API 无 HTML 渲染面）+ frameOptions DENY + HSTS 一年含子域（仅 HTTPS 生效）；默认头（nosniff/Cache-Control/X-XSS-Protection）保持；**actuator 实证结论**：include 白名单四项即钉死暴露面（未列端点不暴露），无冗余 exclude 配置；permitAll 面维持供采集，ECS 安全组源址限制随 Phase 4 簇⑥落地（跨专项协作点登记） |
| B5 | 依赖供应链基线：dependency-check + CycloneDX SBOM | 新增 | ✅ 2026-08-18 闭环（2026-08-17 插件接入 + SBOM 入档；2026-08-18 漏洞扫描执行入档）：根 pom 接入 dependency-check-maven 13.0.0 + cyclonedx-maven-plugin 2.9.3 **不绑生命周期**（显式调用形态，CI 不强制定案口径）；**实证 13.x NVD API key 强制**（无 key UpdateException 无数据源回落，坑位㉜）；SBOM 首跑 425 组件入档 docs/reports/supply-chain/bom-2026-08-17.json；**漏洞扫描 2026-08-18 执行**（用户侧 NVD key 申请到位）：237 依赖扫描 20 命中 84 项（CRITICAL 11 / HIGH 31 / MEDIUM 40 / LOW 2），JSON 母表入档 docs/reports/supply-chain/dependency-check-2026-08-18.json（HTML 渲染视图留 target/ 不入库可再生）；基线建立为项目标，治理另立跟踪（CRITICAL 优先） |

**簇② E2E 交付步骤（不启动服务定案形态，用户部署后自测）**：① **跨域拒止**——部署后以白名单外 Origin 头请求 /api/v1 任意端点（如 `curl -H "Origin: https://foreign.example" -i`），预期响应无 Access-Control-Allow-Origin 头；白名单内 Origin 预检 OPTIONS 返回允许头；② **超限 413**——上传 >50MB 文件预期 413；chat 端点发送 Content-Length >1MB 请求体预期 413 JSON；③ **MCP 连打 429**——MCP 客户端对 search 连打 >120 次/分钟预期 RATE_LIMITED 错误帧 + Prometheus `rag_guardrail_mcp_ratelimited_total` 递增；置 RAG_MCP_AUDIT_ENABLED=true 复跑后 kb_audit_log 可查 mode=mcp 轻行；④ **响应头断言**——`curl -I` 核验 X-Frame-Options DENY / Content-Security-Policy / Strict-Transport-Security 三头在响应中；⑤ **漏洞扫描**——申请 NVD API key（nvd.nist.gov 免费）后执行 aggregate 扫描，报告入档 docs/reports/supply-chain/。**E2E 通过（2026-08-17 用户自测回传）**：功能四项 ①-④ 全达成无问题；⑤ 漏洞扫描 2026-08-18 执行入档（基线 237 依赖 20 命中 84 项 CRITICAL 11 / HIGH 31 / MEDIUM 40 / LOW 2，治理另立跟踪）——**簇② 全项闭环**。

### 安全簇③ PII 扩容与注册表化（任务行，分解依据专项方案 §4.3 C1-C3）

> 验证通道（专项方案 §5.1）：新类型样本双链（对话掩码 + ETL 重入库三库消毒）E2E + 干净集误报回归。无 DB 变更（掩码为纯文本替换，审计语义维持 SUCCESS→null 不变量）。

| # | 任务行 | 对应 | 完成情况 |
|---|---|---|---|
| C1 | 确定性正则批扩容：银行卡（Luhn + 分隔符容忍 + 边界断言）/座机/车牌/IPv4 四类识别器 | E4/S7 | ✅ 2026-08-17：四类全正则可判定落地——银行卡 13-19 位 Luhn 校验（程序化构造校验位测试 + 公开标准测试卡号，20+ 位串边界阻断）/座机 0 区号 3-4 位 + 7-8 位号码（置信度 0.8 启发式形态标注）/车牌 31 省简称 + 发牌代号去 I/O + 常规 5 位与新能源 6 位 + 挂学警港澳尾字符（掩码保留前两字符，词内边界断言）/IPv4 段值 0-255 校验 + 数字点邻接边界断言（五段点分串内部不误配；字母前缀版本串形态为登记在案已知边界，类型开关运维绕开）；既有三类模式与掩码逐字迁入零行为漂移；掩码幂等全型回归 |
| C2 | PiiRecognizer 注册表重构：每类型独立识别器（模式/置信度/掩码策略/enabled 开关），Java 原生对齐 Presidio 语义 | E4/S7 | ✅ 2026-08-17：kb-commons security/pii 包 12 构件（接口/基类/七识别器/注册表/掩码结果/配置族/装配）；TextSanitizer.maskPii 静态 API 退役（归一化与词表匹配保留）；**单一实现源纪律承载体迁至 Spring 单 Bean**——kb-commons 首个 Spring 装配（kb-api/kb-eval 双上下文扫描 com.enterprise.kb 均拾取，eval 零基础设施依赖不破），对话链/ETL/审计/MCP/入口日志五消费方注入同一实例；注册顺序即掩码优先级（PHONE→ID_CARD→EMAIL→BANK_CARD→LANDLINE→LICENSE_PLATE→IPV4，18 位纯数字串先落身份证既有序位保留）；配置族 rag.guardrail.pii.{type}.enabled 七类缺省全开（env 松散绑定覆盖，逐键占位符免设）；指标 rag.guardrail.pii.masked 总项语义不变 + 七类型子项分列（零标签纪律同 output.replaced 形态） |
| C3 | 姓名/地址弱信号登记：NER 依赖默认关，配置开关预留 | S7 原注记 | ✅ 2026-08-17（登记形态）：PiiType 枚举含 NAME/ADDRESS + 配置键 rag.guardrail.pii.name/address.enabled 默认 false 预留；识别器未实现——置 true 启动 warn 提示无效（fail-safe 不阻断启动）；待干净集误报度量后再评估实现，不排期 |
| T5 钩子 | 输出 PII 回显探测接入（簇① T5 预留钩子闭环，专项方案 §4.1 A3 依赖项） | A3/C2 | ✅ 2026-08-17：OutputGuardrailAdvisor 金丝雀校验后、词表判定前经注册表检测视图观察——检出未掩码强形态 PII → rag.guardrail.output.pii.echo 计数 + warn 类型事实，FLAG 观察起步不替换不阻断（验证误报后再定动作）；掩码形态不构成回显（幂等纪律对偶面）；同步 after() 与流式聚合后验双路径接线；测试 +3（同步计数不替换/跨块聚合计一次/掩码形态不计） |
| 门禁 | 干净集 PII 误报回归门禁（验证通道） | A4 同源 | ✅ 2026-08-17：kb-eval cleanRegressionSetHasZeroPiiHits 强制项——七类缺省全集识别器对干净问句（≥102 下限，实际 110）零检出，误报即须收紧模式或关停对应类型开关；门禁口径仅 question（用户输入面），期望答案可合法承载语料形态；识别器单测 22 项 + 装配测试 3 项 |

**簇③ E2E 交付步骤（不启动服务定案形态，与簇② E2E 同批自测）**：① **对话链掩码双链**——部署后发送含四类新形态的消息（银行卡可用公开标准测试卡号 4111111111111111 分隔形态、座机 010-XXXXXXXX、车牌省份简称 + 字母 + 5 位序号、IPv4 内网段形态各一），预期：回答正常 + kb_audit_log.query_text 与 Redis 记忆落脱敏态 + Prometheus `rag_guardrail_pii_masked_bank_card_total` / `_landline_total` / `_license_plate_total` / `_ipv4_total` 各递增；② **ETL 重入库三库消毒**——上传含同四形态的 TXT 文档，预期 kb_chunk.content / 向量库 / ES 均存脱敏态（检索台查 chunk 内容核验）；③ **类型开关**——置 RAG_GUARDRAIL_PII_IPV4_ENABLED=false 重启后 IPv4 形态不再掩码（运维绕开面验证，验毕复原）；④ **输出回显观察**——构造使模型回答中回显强形态 PII 的问法（如要求复述含卡号的上下文），预期回答不被替换 + `rag_guardrail_output_pii_echo_total` 递增 + 日志 warn 类型记录；⑤ **干净集回归**——机器侧门禁已绿（cleanRegressionSetHasZeroPiiHits），用户侧 CI 复跑 kb-eval chain 确认 NR 等既有指标不漂移。**E2E 通过（2026-08-17 用户自测回传）**：①-⑤ 全达成无问题（簇③收官）。

### 安全簇④ 间接注入运行时闭环（任务行，分解全文 `安全簇④任务分解（零字面词执行纪律版）.md`）

> 执行纪律：零字面词沿簇①母本 + 簇④增量条 5-7（毒化正文永不经会话 / 触发问句与 Judge 判据明文安全区 / 警示注记与 Judge Prompt 中性结构文案）。依赖图：D1 → (D2 ∥ D3a) → D3b（用户侧）→ 收尾。

| # | 任务行 | 对应 | 完成情况 |
|---|---|---|---|
| D1 | 运行时扫描与逐条警示：IndirectInjectionScanPostProcessor（同源词表检测视图，BLOCK+FLAG 全档）+ warn/exclude 策略双档（默认 warn）+ 逐条警示渲染（元数据感知编号格式器）+ indirect.flagged/excluded 指标，装配位序 rerank 之前 | E3/G1 | ✅ 2026-08-18：机器侧落地——扫描后处理器（合成词表单测 9 例：双策略/打标/剔除/全剔回落/开关/FLAG 全档/归一化视图/非法策略回落/按条计数）+ 警示渲染零漂移钉死 + 指标 2 项 + 配置族；装配位序定案 rerank 之前（exclude 不进重排与 final TRACE）；审计口径定案不写 guardrail_flags（SUCCESS→null 不变量）；全模块 574 测试绿；设计回写 12 章 §12.8 v2.46。**补记（E2E 启动验证修复）**：双构造器无装配注解致启动失败 → 公开构造器钉 @Autowired（Spring 多构造器无注解回落无参构造器；先例 ContextualEnrichmentTransformer 同形态）；kb-ai-core 200 测试绿，实证登记代码注释，待用户复测 |
| D2 | 入库打标消费：injection_hit=true chunk RRF 融合分衰减降权——默认关（§9 定案④），kb-eval 检索门禁双跑度量后定开关口径 | E3 | ✅ 2026-08-18：机器侧落地——**契约补齐先行**（实证缺口：S4 打标原仅落 PG JSONB，向量/ES 两面未携带 → vectorMetadata 传播 + EsChunkDoc 增字段 + ES 检索透传，存量随重入库窗口消化）+ RrfFusion 融合分衰减（factor 默认 0.5，降权只改融合分排名元数据可溯，rerank 降级路径同语义）+ `rag.retrieval.injection-hit.demoted` 指标；`rag.retrieval.injection-hit.demote.enabled` **默认 false**（kb-eval Recall/MRR 开关双跑度量后定口径）；测试 +8（契约传播/fail-safe/降权重排/缺省零漂移/ES 透传），全模块 582 绿；设计回写 12 章 §12.8 v2.46 二批 |
| D3a | 评估机器侧：indirect-qa.json 编码引用语料模型（唯正文编码，触发问句/Judge 判据明区分层）+ import_poison_corpus.py 带外导入 + IndirectInjectionProbe 探针（打标自洽校验）+ Judge EXECUTED/RESISTED 打分位与抑制率 + 报告小节与基线入档 | G1/12.6 提案 | ✅ 2026-08-18：机器侧落地（12.6 提案实施）——`indirect/indirect-qa.json` 独立目录（与 golden 结构性隔离防主数据集污染）+ IndirectQAPair/Loader 编码引用（正文唯一载荷 Base64+指纹，触发问句/判据明文安全区，解码 fail-fast 只回 id）+ import_poison_corpus.py 带外导入（幂等去重/退役留痕/输出仅计数 ID 指纹，自测通过已净场）+ IndirectInjectionRunner（触发走 eval 链 + **打标自洽双标记校验** injection_hit×indirect_injection_hit 假阴性红灯 + 语料同源自洽 + Judge EXECUTED/RESISTED + 抑制率 + target/indirect-eval{-label} 入档）；执行形态定案：毒化文档上传走生产管道用户侧执行；eval.indirect.enabled 默认关；测试 +9（loader 自洽 5 + 汇总/Prompt 结构 4），全模块 591 绿；设计回写 12 章 §12.8 v2.46 三批 |
| D3b | 用户侧内容注入与首跑：3-5 篇毒化语料带外构造 → 编码并库 → E2E（上传打标→检索触发→逐条警示可见→抑制率首跑基线入档，warn/exclude 双策略读数） | G1 | ✅ 2026-08-18：E2E 通过（上传打标 / 触发问答标中均实测生效）；抑制率首跑基线 **1.000**（RESISTED 30/30，2 文档 × 15 问句，target/indirect-eval-ci-chain.* 入档）+ 主评估 198 例全绿无漂移；探针三红灯归因为口径缺口（详见收尾行与 12 章 §12.8 v2.46 五批），用户定案直接收官、缺口转下冲刺；warn/exclude 双策略对照读数随校准批复跑补 |
| 收尾 | 验证通道 + 文档回写 + §7 纪律自检：全模块绿 + 门禁复跑不破 + 12 章 §12.8 新增 + CLAUDE.md 架构事实 | G1 | ✅ 2026-08-18 全闭环（机器侧 + D3b E2E + 首跑基线 + 缺口登记）：全模块 591 测试绿（D1 +18 / D2 +8 / D3a +9）+ 直接注入双门禁与干净集门禁复跑不破（全 reactor exit 0）+ 12 章 §12.8 新增 v2.46 五批 + README + CLAUDE.md 架构事实。**§7 纪律自检逐条**：✅ 文档零字面载荷（分解/进度/§12.8 仅族系名/结构描述/ID）✅ 词表零新增（D1/D2 消费簇①编码基线同源加载器）✅ 毒化正文零接触（indirect-qa.json 空数组缺省 + import_poison_corpus.py 带外通道，自测以无害占位文本走通道后净场；D3b 用户侧注入 30 例全程带外，机器侧仅回显计数/ID/指纹）✅ 命名中性（类名 Indirect*/指标 indirect.flagged/excluded/配置 indirect.*；语料 ID 前缀 poison- 沿方案术语指语料类别非载荷字面）✅ 测试输入全合成（占位词项/占位文本程序化构造）✅ 报告/日志输出仅 ID/布尔/verdict/计数。**D3b E2E 通过（用户自测回传）**：上传打标与触发问答标中均实测生效；抑制率首跑 1.000（RESISTED 30/30）+ 主评估 198 例全绿无漂移；探针三红灯源码级归因为口径缺口（语料自洽前缀变换全失配 / anyMatch 语义过度宣称 / poison-26/28/30 三例运行时✓入库✗前缀偶中假设待查证）——用户定案直接收官，校准方案（前缀剥离比对 + 逐 chunk 打标语义 + eval.indirect.only 开关）登记转下冲刺 |


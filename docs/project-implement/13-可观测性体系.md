# 第十三章：可观测性体系

> 本章为《企业知识库 RAG Agent 工作台：Spring AI 2.0 全景实现报告》v2 拆分版的一部分（原第五卷「核心模块技术实现（实现层）」第十三章）
>
> [📑 返回目录](./README.md) · 最后更新：2026-08-25 · v2.65（Phase 5 簇③：语义缓存指标族启用 + `kb.cache.semantic` span + N1 遥测面裁决）第七次修订
>
> **v2.64（2026-08-23，Phase 5 簇① 机器侧：rerank 观测增强清偿）**：v2.32 ④「补 observation
> 属观测增强另行留档」登记项落地——`RerankDocumentPostProcessor` 裸 RestClient 调用经
> `Observation`（名 `kb.rerank`，contextual `rerank {model}`，低基数 `rerank.model` +
> 高基数 `rerank.candidates`）包裹，`observeChecked` 自动 start/scope/error/stop，异常经既有
> catch 走降级、容错语义零变更。寻父 = registry 当前观测——RAA 双执行器传播包裹（v2.32 ④）
> 在场时挂检索链观测树之下（合树前提已备），寻父落空降级独立 span 不阻断。注入位
> `ObjectProvider<ObservationRegistry>` 缺省回落 NOOP（kb-eval 零观测上下文零影响，同
> SemanticInjectionAdvisor 先例）。**v2.31 ④ 残余登记至此清偿**（rerank 侧；embedding 侧
> 为 Spring AI 框架内置观测、不产碎片问题既有定案不变）。单测 +2（观测形态钉死 + 父观测
> 合树契约），kb-ai-core 242 绿。E2E 观察点：Langfuse 检索链树下应现 `rerank qwen3-rerank`
> span（用户侧随下轮运行态核验）。
>
> **v2.58（2026-08-22，Phase 4 簇⑥ 批4：trace 残余修复 + 双供应商 SLA 监控）**：
> ① **流式主生成 POST 独立 trace 残余（v2.32 ④ 登记项）源码级定案修复**：根因链——
> Spring AI 2.0 GA 的 chat_model 流式观测只经 Reactor Context 传播（internalStream
> `contextWrite` 写入父观测键，不在订阅线程开 ThreadLocal 作用域），而模型 HTTP 层
> （`SpringAiOpenAiHttpClient` = OkHttp + `OkHttpObservationInterceptor`）按**线程当前
> 观测**寻父；POST 的 enqueue 发生在 `Flux.create` 消费者内（订阅瞬间执行，
> `FluxCreate.subscribe` 只对下游订阅者做上下文恢复、消费者本身不恢复），该处
> ThreadLocal 为空，OkHttp dispatcher（已经 `ContextExecutorService.wrap`）捕获空快照 →
> POST span 成独立 trace root。修复落 `SmartRoutingChatModel.stream()`：订阅期经
> Reactor Context 取出父观测（chat_client 观测）开为作用域包裹同步订阅传播
> （`openParentScopeOnSubscribe`，匿名 Flux 子类覆写 subscribe），enqueue 时刻 TL 有父
> → POST span 挂回 chat_client 之下与 gen_ai 生成 span 同层合树；备用重订阅独立再包。
> 单测钉死机制（订阅线程 TL 空 → Flux.create 消费者内可见父观测）；**E2E 合树裁决
> 留用户侧**（M1 可选加练 + Jaeger/Langfuse 单 trace 核验）。同步 call() 路径无此问题
> （拦截器在调用线程同步执行，寻父正常）。
> ② **双供应商 SLA 计数族 +3**（AiBusinessMetrics，接线点 SmartRoutingChatModel
> 熔断态变更与路由分支）：`rag.routing.circuit.opened`（OPEN 转入，达阈首开 + HALF_OPEN
> 试探失败重开）/ `circuit.half-opened`（窗口结束后主模型试探）/ `fallback.invoked`
> （备用接管，OPEN 直发 + 失败即切）。
> ③ **SLA 面板**：`infra/grafana/dashboards/kb-rag-supplier-sla.json`（第五面板：
> 熔断事件时序柱图 / 接管速率 vs 请求总速率 / 主模型降级占比 / 口径说明 text）。
> ④ **告警 13 → 14 条**：+`KbPrimaryModelDegraded`（`increase(circuit_opened[5m]) > 0`，
> for=0m，warning——OPEN 即降级备用，试探成功自动恢复）；合成单测 7 → 8（自检矩阵
> 14 = 6 真触发 + 8 合成）。
> ⑤ **ECS 安全组源址限制说明交付**（12.9 B4 / 17.3 actuator 行清偿）：8090 收敛可信源 +
> 监控端口办公源 + 基础设施端口零公网，执行与回传登记用户侧清单 **SG1**（17.3 台账同步）。
>
> **v2.56（2026-08-20，Phase 4 簇⑥ 批2：监控栈生产化）**：
> ① **13.8 新增**：docker-compose.monitoring.yml 生产化——四服务（Prometheus/Grafana/
> Jaeger/node-exporter）restart + healthcheck（可做的）+ 资源限额 + 日志轮转；Grafana
> 口令经 infra/.env 注入（`GRAFANA_ADMIN_PASSWORD` :? 守卫，明文 admin/admin 形态终结）；
> node_exporter v1.9.1 入栈（host pid + / 只读挂载官方形态）喂 Host 层指标。
> ② **13.6 告警 11 → 13 条**：kb-rag-host 组激活（HostDiskNearFull / HostMemoryNearFull，
> 簇① 注释留档转正）。**自检矩阵定案**：6 条真触发演练 + 7 条 promtool 合成单测
> （infra/prometheus/alert-selfcheck/，数值构造防 increase() 外推歧义），执行形态见
> 用户侧待执行项清单.md M1。
> ③ **实证留档**：Jaeger all-in-one scratch 基座容器内无 shell/curl——in-container
> healthcheck 不可行，自愈靠 restart 策略 + OTLP 导出侧容错。
> ④ **开机自启**：infra/kb-monitoring.service（oneshot + RemainAfterExit，与 kb-api.service
> 同职责分层定案）。
>
> **v2.32（2026-08-15，Phase 4 簇② 面板与统计：4.3/4.6 落地 + v2.31 残余 trace 修复定案）**：
> ① **13.4 面板定稿**：infra/grafana/dashboards/ 四面板 JSON（概览/检索/LLM/业务，
> 21 面板 36 表达式，全部按 actuator 实证名——沿用 13.6 命名口径）；**无指标支撑
> 面板定案不设**，理由入各面板口径说明 panel：活跃用户数（零标签纪律，租户级走
> kb_audit_log/Redis 账本）、Bad Case 分布（簇④ 审计 API 承接）、向量/BM25 单路
> 延迟（无独立 Timer，trace 侧可见）、缓存命中率（语义缓存 Phase 5.6）。
> ② **资产落位**：infra/prometheus/prometheus.yml（rule_files 接线 13.6 告警；
> scrape 本地核验形态 host.docker.internal:8090，簇⑥ ECS 部署改 compose 服务名）+
> infra/grafana/provisioning/（datasource **uid=prometheus 钉死**，面板 JSON 以此引用）+
> infra/docker-compose.monitoring.yml（Prometheus v2.55.1 / Grafana 11.5.2 / Jaeger
> all-in-one 1.62.0 三件套——本地自核验形态，生产加固归簇⑥；Jaeger 兼作 trace
> 类修复的 OTLP 验证后端，不依赖 Langfuse 凭据）。
> ③ **AiBusinessMetrics +rag.ttft**（Timer p50/p95/p99，簇② 4.3）：AgentController
> 流式路径请求进入 → 首个非空 token，双链共记零标签；**retrieval.latency
> publishPercentiles 补齐**（v2.11 草图承诺、实现期遗漏，面板消费分位时实证发现）。
> ④ **trace 残余修复定案（v2.31 ④ 关闭大半）**：根因 = RAA 双执行器
> （retrievalExecutor/hybridRetrievalExecutor）裸虚拟线程逃逸观测上下文 →
> **两级传播包裹**：retrievalExecutor 经 ContextPropagatingTaskDecorator（匿名委托
> 包裹——VirtualThreadTaskExecutor 无 decorator 钩子，源码实证）、hybridRetrievalExecutor
> 经 ContextExecutorService.wrap。源码级实证链：ObservationThreadLocalAccessor.setValue
> = openScope（真实作用域非裸 TL 赋值）；SimpleObservationRegistry 当前观测存储为
> **静态 ThreadLocal**（跨实例共享——accessor 自带 registry 与业务 registry 异实例
> 仍 capture/restore 成立）；accessor 经 ServiceLoader 静态自动装载（坑位㉖ 延续）。
> **E2E 裁决（本地 Jaeger OTLP）**：单 trace 32 span 合树，embedding×2 及其 HTTP POST
> 父链 retrieval_gate → … → http 根 ✓。**rerank 源码核验**：裸 RestClient.builder()
> 不产 observation——无碎片问题（簇① 所见独立 POST 实为主生成调用）；补 observation
> 属观测增强另行留档。**新残余一项留簇⑥**：流式主生成模型 HTTP POST 仍独立 trace
> （模型实现深层线程跳跃），候选修复 Hooks.enableAutomaticContextPropagation()
> 全局钩子回归面大，随簇⑥ 生产加固全量回归时评估。
> ⑤ **4.6 统计 API**：GET /api/v1/stats/overview（文档总数/状态五值分布/chunk 总量/
> 路由分布/14 天入库趋势补 0）+ GET /api/v1/stats/documents/processing（处理中三态
> 计数+清单）；租户守卫同 FeedbackController（IDENTITY_INCOMPLETE）；聚合走
> kb_document 单表（chunk 取文档侧 chunk_count 口径，软删精确口径属簇③）。
> ⑥ **实证坑登记**：a) `spring-boot:run -pl kb-api` 静默执行兄弟模块**已安装旧 jar**
> ——本次 trace 修复因此被吞一轮（修复后须先 install 变更模块再起，fat jar 直起更稳）；
> b) 同款 spring-boot:run 命令偶发丢失 `-Dspring-boot.run.jvmArguments`（根因未查明，
> fat jar `java --enable-preview -jar` 直起规避）。
> ⑦ **自核验链（本地 compose）**：promtool check config + targets up + 36/36 面板
> 表达式对活 Prometheus 校验 + Grafana datasource/四面板 provisioning 加载 + 真实
> 流量喂数（TTFT/检索分位/token/路由/护栏全活）+ Jaeger 合树裁决。ECS 部署归簇⑥。
>
> **v2.31（2026-08-14，trace 碎片化根因定案与修复，v2.30 留档项⑥ 关闭）**：
> ① **根因（源码级定案，双因叠加）**：a) `ChatClient.builder(chatModel)` 单参重载
> 默认传 `ObservationRegistry.NOOP`（ChatClient 接口源码实证）——ragAgentChatClient/
> toolAgentChatClient 的 chat_client 与全部 Advisor 观测静默 NOOP，主链树整体缺失，
> 仅剩模型层裸 span（模型观测经 SmartRoutingConfig 显式装配真实 registry 幸存）；
> b) `BaseAdvisor.adviseStream` 模板将 before() 与下游链 `publishOn(boundedElastic)`
> ——请求线程 ThreadLocal 作用域不再可达，模型层观测按「当前观测」寻父落空成根 span。
> 簇① 时的 Reactor 传播/accessor 装载修复并非无效而是未触及主因（NOOP 观测根本不读上下文）。
> ② **修复**：rag/tool 双链 ChatClient 构建改 `ChatClient.builder(chatModel,
> observationRegistry, null, null)` 显式装配应用 registry；AgentController.chatStream
> 请求线程捕获当前观测经 `contextWrite` 写入 Reactor Context 的
> `ObservationThreadLocalAccessor.KEY`——Spring AI 2.0 流式链经 deferContextual
> 显式读该键作父观测（源码契约，不依赖 ThreadLocal 自动恢复），确定性兜底。
> ③ **E2E 实证（Langfuse）**：http post /api/v1/chat/stream 单 trace 28 观测合树——
> http 根 → security → chat_client（parent=secured request 核验）→ audit_trace/
> token_budget/rate_limit/output_guardrail/input_sanitize/message_chat_memory/
> query_routing/retrieval_trace/retrieval_gate 全 Advisor 链 + 主/分类双 GENERATION。
> ④ **残余限制（留档，不阻塞）**：embedding×2 与 rerank POST 仍独立 trace——RAA
> 检索任务经裸 executor 虚拟线程跳跃，逃逸 Reactor 上下文恢复，检索层观测寻父落空；
> 随簇②/⑥ 观测专项评估修复（候选：检索任务 Observation.Scope 包裹）。
> ⑤ 坑位登记：㉗ ChatClient.builder 单参 NOOP、㉖ 修正（原疑点 MVC 作用域生命周期
> 证伪——MVC 快照回写与订阅捕获均有效）。
>
> **v2.30（2026-08-14，Phase 4 簇① 观测地基实证回写）**：
> ① 13.1 配置形态修正——Boot 4.1 中 `management.otlp.tracing.*` 已迁移至
> `management.opentelemetry.tracing.export.otlp.*`（jar 元数据实证）；OTLP 导出链
> 依赖三件套（spring-boot-micrometer-tracing-opentelemetry + micrometer-tracing-otel
> 桥 + opentelemetry-exporter-otlp）；内容捕获开关落 `spring.ai.chat.observations` /
> `spring.ai.chat.client.observations` / `spring.ai.tools.observations` 三层（默认全 false，
> 项目经 `RAG_OBSERVABILITY_LOG_CONTENT` 统一置 true）。
> ② 13.5 Langfuse 形态修正——自托管 Docker 改为 **Cloud 免费档**（自托管需 7 vCPU/17.5GB
> 超 ECS 约束；免费档 50k observations/月、2 用户、30 天保留，SaaS 出境合规风险用户已知悉）；
> OTLP 契约定稿（EU endpoint + Basic Auth + 仅 HTTP/protobuf）。
> ③ 新增 13.6 告警规则落档（4.2）：infra/prometheus/alert-rules-kb-rag.yml 11 条，
> Prometheus 指标名实证口径（`.total` 结尾 counter 不重复追加 `_total` 后缀）。
> ④ AiBusinessMetrics 新增 5 计数（rag.request.total/rejected/error + rag.rerank.total/fallback），
> 13.3 清单随之扩充。
> ⑤ **内容桥接实证（E2E 三轮迭代定稿）**：Spring AI 2.0 GA 的 log-prompt/log-completion
> 只打应用日志不进 span（TracingAwareLoggingObservationHandler 是纯日志 handler 包装）→
> Langfuse generation input/output 恒空；打标型 ObservationHandler 与 span 结束存在 onStop
> 顺序竞态（实测静默丢失）→ 定稿 `ContentCapturingChatModelObservationConvention`
> （继承 Default convention 追加 gen_ai.prompt/gen_ai.completion 高基数 KeyValue，
> tracing handler 落 attribute 时序确定），SmartRoutingConfig 双模型经 ObjectProvider
> 条件接线，E2E 实证 Langfuse 侧完整 prompt/completion 可见。
> ⑥ **~~未解决项（簇① 留档）~~ 已于当日 v2.31 定案修复**：trace 碎片化根因为
> ChatClient.builder 单参重载默认 NOOP registry（主链观测整体缺失）+ BaseAdvisor
> publishOn 线程跳跃（模型层观测寻父落空），疑点「MVC 异步 SSE 作用域生命周期」证伪。
> 修复与 E2E 实证见 v2.31 注记①-④；残余限制（embedding/rerank 检索层 span 独立
> trace）随簇②/⑥ 专项评估。
>
> **v2 修订**：13.2 `AuditTraceAdvisor.after()` 与 `saveAuditLog` 内 `response.response()` 统一修正为 `response.chatResponse()`；新增 13.5 小节引入 Langfuse 作为 LLM 原生可观测层，与 Prometheus/Grafana 基础设施层形成双层架构。

### 13.1 Spring AI 原生观测配置

**v2.30 落码形态**（依赖 + 配置；仅 kb-api 宿主引入，kb-eval 零影响）：

```xml
<!-- Observation → OTel 桥接 → OTLP 导出三件套（版本经 Boot BOM 管理） -->
<dependency>org.springframework.boot:spring-boot-micrometer-tracing-opentelemetry</dependency>
<dependency>io.micrometer:micrometer-tracing-bridge-otel</dependency>
<dependency>io.opentelemetry:opentelemetry-exporter-otlp</dependency>
```

```yaml
# application.yml（kb-api）
spring:
  ai:
    chat:
      observations:                     # ChatModel 层内容捕获（jar 元数据实证，默认全 false）
        log-prompt: ${RAG_OBSERVABILITY_LOG_CONTENT:true}
        log-completion: ${RAG_OBSERVABILITY_LOG_CONTENT:true}
        include-error-logging: true
      client:
        observations:                   # ChatClient 层内容捕获
          log-prompt: ${RAG_OBSERVABILITY_LOG_CONTENT:true}
          log-completion: ${RAG_OBSERVABILITY_LOG_CONTENT:true}
    tools:
      observations:
        include-content: ${RAG_OBSERVABILITY_LOG_CONTENT:true}   # 工具调用内容

management:
  tracing:
    sampling:
      probability: ${TRACING_SAMPLING:1.0}     # 全量采样（当前流量口径；可调低）
    export:
      otlp:
        enabled: ${TRACING_OTLP_ENABLED:false} # 总开关默认关——未配凭据零导出零噪音
  opentelemetry:
    tracing:
      export:
        otlp:
          # v2.30 实证：Boot 4.1 属性前缀已由 management.otlp.tracing.* 迁至此处；
          # endpoint 缺省 None 时自动配置不创建导出器。
          # 实证坑：本属性为「信号专属 endpoint」，OTel Java SDK 原样使用不追加
          # /v1/traces（仅 base endpoint 才追加）——必须带全路径，缺则 Langfuse 404
          endpoint: ${LANGFUSE_OTLP_ENDPOINT:https://cloud.langfuse.com/api/public/otel/v1/traces}
          transport: http               # Langfuse 仅收 HTTP/protobuf，勿用 gRPC
          headers:
            Authorization: ${LANGFUSE_OTLP_AUTH:}   # Basic base64(pk:sk)
```

指标命名规范：Spring AI 2.0 的 ChatClient 操作指标名为 `spring.ai.chat.client.operation`（非 duration 后缀）；Token 用量键为 `gen_ai.usage.input_tokens` / `gen_ai.usage.output_tokens`。

**Prometheus 侧指标名实证口径（v2.30）**：Micrometer 经 Prometheus registry 导出时点转下划线、counter 追加 `_total` 后缀，但原名已以 `total` 结尾的不重复追加——`rag.request.total → rag_request_total`（单 total）、`rag.request.error → rag_request_error_total`（追加）。告警表达式按 `/actuator/prometheus` 实证名校准，勿凭命名规约推演。

> **⚠️ OTel GenAI 语义约定实验态风险（v2 注）**：截至 2026 年中，OpenTelemetry GenAI semantic conventions（`gen_ai.*` 属性族）仍处于 **experimental** 状态，GA 定稿前键名可能发生 breaking change。应对策略：① Dashboard/告警中不直接硬编码 `gen_ai.*` 键名，经 recording rule 或指标映射层间接引用；② 跟踪 semantic-conventions-genai 仓库的 stable 化进度，GA 后统一迁移；③ Langfuse 侧属性映射同样集中在配置层管理。

### 13.2 AuditTraceAdvisor

```java
/**
 * 全链路审计 Advisor —— 继承 BaseAdvisor，before()记录开始时间，after()持久化审计日志
 */
@Component
public class AuditTraceAdvisor extends BaseAdvisor {
    
    private final KbAuditLogRepository auditLogRepository;
    private final MeterRegistry meterRegistry;
    
    // 使用 ThreadLocal 在 before/after 之间传递计时起点
    private final ThreadLocal<Long> startTimeHolder = new ThreadLocal<>();
    private final ThreadLocal<String> traceIdHolder = new ThreadLocal<>();
    
    @Override
    public ChatClientRequest before(ChatClientRequest request, AdvisorChain chain) {
        startTimeHolder.set(System.currentTimeMillis());
        traceIdHolder.set(Span.current().getSpanContext().getTraceId());
        return request;
    }
    
    @Override
    public ChatClientResponse after(ChatClientResponse response, AdvisorChain chain) {
        long latency = System.currentTimeMillis() - startTimeHolder.get();
        String traceId = traceIdHolder.get();
        
        // 异步落库审计日志
        saveAuditLog(traceId, response, latency);
        
        // 记录指标
        Timer.builder("rag.request.latency")
            .tag("outcome", "success")
            .register(meterRegistry)
            .record(latency, TimeUnit.MILLISECONDS);
        
        startTimeHolder.remove();
        traceIdHolder.remove();
        return response;
    }
    
    @Async("etlExecutor")
    protected void saveAuditLog(String traceId, ChatClientResponse response, long latency) {
        KbAuditLogEntity log = KbAuditLogEntity.builder()
            .traceId(traceId)
            .finalAnswer(response.chatResponse().getResult().getOutput().getText())
            .latencyMs((int) latency)
            .build();
        auditLogRepository.save(log);
    }
    
    @Override
    public int getOrder() { return 10; }   // v2 Order 重排：审计最外层，见第十一章 11.2
}
```

### 13.3 AiBusinessMetrics

> **v2.11 实现期定稿（2026-08-07，任务 3.13）**：草图类落地为
> `kb-ai-core/metrics/AiBusinessMetrics`（第六章预留包位），全项目业务指标集中
> 注册（TokenBudgetAdvisor 原分散的 rag.token.* 一并收编）。落地范围与草图差异：
> ① **已落地**——rag.feedback.like/dislike（3.17 反馈 API 已接线，v2.14：按受理提交计数）、
> rag.retrieval.total/hit（AuditTraceAdvisor 按 final trace 非空计命中；内层
> 护栏/限流提前拒绝未达检索**不计入分母**）、rag.retrieval.latency
> （HybridDocumentRetriever 真实耗时 Timer，Prometheus 侧出 p50/p95/p99）、
> rag.tool.call.total/success/pending（AuditTraceAdvisor 按 ToolCall.status 分桶，
> 状态常量统一至 RetrievalContext.ToolCall）、rag.token.total/budget.rejected；
> ② **不注册**——rag.retrieval.cache.hit（语义缓存未实现，Phase 5.6）、
> rag.llm.*（模型层计数随 Phase 4 可观测增强）；
> ③ **采集放行**——SecurityConfig 追加 /actuator/prometheus、/actuator/metrics/**
> permitAll（与 health/info 同级；生产加固可选 IP 白名单独立 FilterChain）；
> ④ 标签纪律延续：全部指标不带租户标签（防基数膨胀）。
>
> **v2.30 扩充（Phase 4 簇①）**：新增 5 计数——`rag.request.total/rejected/error`
> （AuditTraceAdvisor 与审计三态同语义旁路计数：BusinessException → rejected、其他异常 →
> error；为 4.2 告警拒绝率/错误率分母）+ `rag.rerank.total/fallback`
> （RerankDocumentPostProcessor 运行时执行/降级计数；endpoint 未配置的静态降级不计入，
> 配置态非运行态）；同批兑现 v2.11 ② 的 rag.llm.* 遗留口径：模型层调用观测改由
> Observation → OTel span 链承接（Langfuse trace 树），不再另立 rag.llm.* counter。
> 另有 routing.chitchat/knowledge（5.4）与 guardrail.*（簇⑤）、document.reindex.*（簇⑥）
> 各期已入清单。
>
> **v2.32 扩充（Phase 4 簇②）**：新增 `rag.ttft` Timer（p50/p95/p99）——流式首 Token
> 延迟（请求进入 → 首个非空 token），AgentController 双链共记零标签；**补齐
> rag.retrieval.latency 的 publishPercentiles(0.5/0.95/0.99)**——v2.11 草图承诺、
> 实现期遗漏，簇② 面板消费分位时实证发现（此前 Prometheus 侧仅 count/sum/max）。
>
> **v2.58 扩充（Phase 4 簇⑥ 批4）**：双供应商 SLA 计数族 +3——
> `rag.routing.circuit.opened`（熔断 OPEN 转入：达阈首开 + HALF_OPEN 试探失败重开均计）/
> `rag.routing.circuit.half-opened`（熔断窗口结束后首个请求试探主模型）/
> `rag.routing.fallback.invoked`（备用模型接管：OPEN 直发 + 失败即切双路径）；接线点
> SmartRoutingChatModel 熔断态变更与路由分支（kb-eval 域经 SimpleMeterRegistry 隔离
> 实例，不串生产账）。零标签纪律延续；消费方 = kb-rag-supplier-sla 面板 +
> KbPrimaryModelDegraded 告警（主模型可用率与切换时序的指标底座）。
>
> **v2.65 扩充（Phase 5 簇③）**：① **兑现 v2.11 ② 的 `rag.retrieval.cache.hit`
> 预留位**（语义缓存，11.9）——计数族 +3：`rag.retrieval.cache.hit/miss`
> （SemanticCacheService KNN 查找判定：达阈计 hit / 低于阈值·空索引·运行期故障
> fail-open 均计 miss）+ `rag.retrieval.cache.invalidated`（按文档失效删除条数计数）；
> 命中率 = hit/(hit+miss)，验收对照 08 章簇③（>30% 真实流量 + 命中延迟 P95 降
> >40%）；② **`kb.cache.semantic` span**（批2，对齐 `kb.rerank` 命名族）——
> CacheCheckAdvisor 嵌入+查找经 Observation 包裹（低基数 `cache.outcome`=hit/miss
> + 高基数 `cache.tenant`，ObjectProvider NOOP 降级），挂 chat_client 观测树；
> ③ **N1 遥测面裁决（11.10）**——DeepSeek `prompt_cache_hit_tokens/miss_tokens`
> **不经 Spring AI 标准 Usage 映射面进入应用层遥测**（审计/指标零缓存可见性），
> 零代码不新增自定义映射耦合；观测面 = 用户侧 E2E 直调对照 + DeepSeek 控制台
> 账单。预留位清单余 `rag.llm.*`（v2.30 已判由 span 链承接，不另立）。

```java
/**
 * AI 业务指标采集
 */
@Component
public class AiBusinessMetrics {
    
    private final MeterRegistry meterRegistry;
    
    // 检索指标
    private final Timer retrievalLatency;
    private final Counter retrievalTotal;
    private final Counter retrievalCacheHit;
    
    // LLM 指标
    private final Counter llmCallTotal;
    private final Counter llmTokenTotal;
    private final Counter llmErrorTotal;
    
    // 业务指标
    private final Counter feedbackLike;
    private final Counter feedbackDislike;
    private final Counter toolCallSuccess;
    
    public AiBusinessMetrics(MeterRegistry meterRegistry) {
        this.meterRegistry = meterRegistry;
        
        // 检索指标
        this.retrievalLatency = Timer.builder("rag.retrieval.latency")
            .description("混合检索延迟（毫秒）")
            .publishPercentiles(0.5, 0.95, 0.99)
            .register(meterRegistry);
        this.retrievalTotal = Counter.builder("rag.retrieval.total")
            .description("检索总次数")
            .register(meterRegistry);
        this.retrievalCacheHit = Counter.builder("rag.retrieval.cache.hit")
            .description("语义缓存命中次数")
            .register(meterRegistry);
        
        // LLM 指标
        this.llmCallTotal = Counter.builder("rag.llm.call.total")
            .register(meterRegistry);
        this.llmTokenTotal = Counter.builder("rag.llm.token.total")
            .register(meterRegistry);
        this.llmErrorTotal = Counter.builder("rag.llm.error.total")
            .register(meterRegistry);
        
        // 业务指标
        this.feedbackLike = Counter.builder("rag.feedback.like")
            .register(meterRegistry);
        this.feedbackDislike = Counter.builder("rag.feedback.dislike")
            .register(meterRegistry);
        this.toolCallSuccess = Counter.builder("rag.tool.call.success")
            .register(meterRegistry);
    }
    
    // 便捷记录方法
    public void recordRetrieval(long latencyMs) {
        retrievalLatency.record(latencyMs, TimeUnit.MILLISECONDS);
        retrievalTotal.increment();
    }
    
    public void recordFeedback(boolean isPositive) {
        if (isPositive) feedbackLike.increment();
        else feedbackDislike.increment();
    }
    
    // 指标导出端点
    @ReadOperation
    public Map<String, Object> getSummary() {
        return Map.of(
            "total_llm_calls", llmCallTotal.count(),
            "total_tokens", llmTokenTotal.count(),
            "error_rate", llmErrorTotal.count() / Math.max(1, llmCallTotal.count()),
            "feedback_positive_rate", feedbackLike.count() / 
                Math.max(1, feedbackLike.count() + feedbackDislike.count()),
            "cache_hit_rate", retrievalCacheHit.count() / Math.max(1, retrievalTotal.count())
        );
    }
}
```

### 13.4 Grafana 大盘设计（v2.32 定稿，Phase 4 簇② 任务 4.3；v2.58 批4 +SLA 盘）

草图经指标可用性核验后定稿——**面板只设已有指标支撑项**，无支撑项定案不设并入
各面板「口径说明」panel（防「面板有框无数」误导运维）。落档
`infra/grafana/dashboards/` 五 JSON（provisioning 加载，uid 钉死见 13.7），刷新 30s：

| Dashboard（uid） | 定稿面板 | 草图删减项与去向 |
|-----------|---------|---------|
| **概览**（kb-rag-overview） | 问答入口 QPS、请求延迟 max、请求三态比率（拒绝/错误）、Token 每小时增量、TTFT P95/P50 | 活跃用户数——零标签纪律无指标，租户级观测走 kb_audit_log/Redis 账本 |
| **检索**（kb-rag-retrieval） | 检索延迟 p50/p95/p99（簇② 补齐 publishPercentiles 后可用）、命中率/空检索率、意图路由分流、rerank 执行/降级率 | 向量/BM25 单路延迟——无独立 Timer（降级矩阵内部值，trace 侧可见）；缓存命中率——语义缓存 Phase 5.6；召回 Chunk 数分布——需标签维度，违反基数纪律 |
| **LLM**（kb-rag-llm） | TTFT 分位（p50/p95/p99）、Token 速率 + 预算拒绝速率、请求错误率 | Token 按模型/租户、模型调用次数、Fallback 次数——v2.30 口径模型层观测由 Langfuse trace 链承接，rag.llm.* 不注册；主备熔断经错误率 + 审计 ERROR 态观察 |
| **业务**（kb-rag-business） | 反馈趋势、好评比（1h）、工具调用三态、护栏命中五列、文档重入库三态 | Bad Case 分布——簇④ 审计 API 承接；知识库入库量——走 4.6 统计 API（前端仪表盘簇④ 消费），非 Prometheus 指标 |
| **双供应商 SLA**（kb-rag-supplier-sla，v2.58 批4） | 熔断事件时序（OPEN 转入 / HALF_OPEN 试探柱图）、备用接管速率 vs 请求总速率、主模型降级占比（1h 接管/请求，clamp_min 防除零）、口径说明 text | 全族消费批4 新增 rag.routing.circuit.* / fallback.invoked；LLM 面板原「Fallback 次数」删减项至此以事件计数形态落地（模型调用明细仍走 Langfuse） |

面板表达式全部按 13.6 实证命名口径（点转下划线；`.total` 结尾 counter 不重复追加
`_total`），簇② 本地自核验 36/36 表达式对活 Prometheus 校验通过。

### 13.5 LLM 原生可观测层：Langfuse（v2 新增）

2026 年企业 RAG 可观测规范为双层架构：

| 层 | 工具 | 职责 |
|---|---|---|
| LLM 原生层 | **Langfuse Cloud 免费档**（v2.30 定案） | LLM trace 树、prompt 调试、评估数据集、按 prompt/租户成本归因 |
| 基础设施层 | Prometheus + Grafana（保留 13.4） | QPS、P95 延迟、错误率、资源占用 |

**v2.30 选型修正**：v2 原案为自托管 Docker，但 Langfuse v3 自托管需 7 vCPU / 17.5GB 内存，超出 ECS 单机约束；改选 **Langfuse Cloud 免费档**（50k observations/月、2 用户、30 天保留）。trace 数据出境的 SaaS 合规风险已告知用户并接受；重估触发条件为合规要求变化或流量超免费档，届时切 Phoenix 自托管备选（资源需求已在档）。

**接入方式**：OTel OTLP 导出，无代码侵入。OTLP 契约定稿（v2.30 实证）：

```yaml
management:
  opentelemetry:
    tracing:
      export:
        otlp:
          # EU Cloud endpoint（US 区为 https://us.cloud.langfuse.com/api/public/otel/v1/traces）；
          # 实证：信号专属 endpoint 原样使用不追加路径，必须带全路径 /v1/traces（缺则 404）
          endpoint: ${LANGFUSE_OTLP_ENDPOINT:https://cloud.langfuse.com/api/public/otel/v1/traces}
          transport: http               # Langfuse 仅收 HTTP（protobuf/json），不支持 gRPC
          headers:
            Authorization: ${LANGFUSE_OTLP_AUTH:}   # Basic base64(public_key:secret_key)
```

**备选方案**：Arize Phoenix（开源、可本地部署，嵌入/检索分析见长；自托管资源需求约 1 vCPU / 1-2GB SQLite 起步）同为 2026 主流 LLM 原生可观测平台，接入方式类似（OTel 导出）。选型上 Langfuse 作主选（Spring AI 官方集成、prompt 管理 + 评估数据集完整），Phoenix 作流量超免费档或检索质量深度分析时的自托管补充，避免单一平台锁定。

**Arconia 约定映射层（v2.30 登记备用）**：`gen_ai.*` 语义约定仍处 experimental，Arconia 框架（Spring AI 核心贡献者维护，0.28+ 支持 Boot 4.1 / Spring AI 2.0）提供单配置属性 `arconia.observations.conventions.opentelemetry.ai.flavor`（opentelemetry/openlit/openllmetry/langsmith）切换 GenAI 约定 schema。当前 Langfuse 原生解析 OTel GenAI 约定，无需引入；待键名 breaking change 或切换可观测后端时作为映射层接入，对应 13.1 风险应对策略①。

Langfuse 同时承接 kb-eval 的评估运行记录（见第 16 章），与检索调试台（第 10 章）共享 trace 数据源。

### 13.6 Prometheus 告警规则（v2.30 新增，Phase 4 任务 4.2；v2.56 批2 Host 层转正；v2.58 批4 +SLA）

落档 `infra/prometheus/alert-rules-kb-rag.yml`，共 **14 条**，分三组：

| 组 | 规则 | 口径说明 |
|---|---|---|
| **Host 层**（2，v2.56 转正） | HostDiskNearFull / HostMemoryNearFull | 簇① 注释留档转正——node_exporter v1.9.1 入监控栈后激活；根分区 >85% / 可用内存 <10%，各 for 10m |
| **JVM/进程**（4） | KbProcessRecentlyRestarted / KbJvmHeapNearFull / KbJvmGcPressureHigh / KbThreadsDeadlocked | actuator 原生指标即可覆盖 |
| **LLM 特有**（8） | KbFeedbackLikeRatioLow / KbEmptyRetrievalRateHigh / KbRejectionRateHigh / KbRequestErrorRateHigh / KbTokenSpike / KbRerankFallbackRateHigh / KbQuotaHitRateHigh / **KbPrimaryModelDegraded**（v2.58 批4） | 全部映射 AiBusinessMetrics 既有/簇①新增指标；阈值取规划调研参考值（好评比 <70% / 空检索 >10% / 拒绝率 >15% / 错误率 >5% / Token 突增 / 降级率 >10% / 配额 >3%），各附分母下限防小样本误报；KbPrimaryModelDegraded = `increase(circuit_opened[5m]) > 0`（for=0m，OPEN 即降级备用，HALF_OPEN 试探成功后窗口滑出自恢复） |

**两处实证修正**：① Token 突增调研参考值 >2σ，PromQL 无法直接计算标准差，落 `increase(1h) > 3 ×（24h 增量/24）` 近似，注明校准锚点；② `jvm_threads_deadlocked` 仅在死锁数 >0 时由 Micrometer 条件注册——序列缺席即无死锁，规则静默为正确态（注释在档）。

**验证口径（v2.56 批2 清偿 4.2 欠账）**：簇① 以 `promtool check rules` / YAML 结构自检验证语法；真实触发自检随批2 生产化一并交付——**自检矩阵 14 条全覆盖**（v2.58 批4 +1）：6 条真触发演练（KbProcessRecentlyRestarted 重启容器 / KbEmptyRetrievalRateHigh 空语料租户 / KbRejectionRateHigh BLOCK 词项 / KbRequestErrorRateHigh 临时无效主模型 key / KbRerankFallbackRateHigh 临时无效 rerank 凭据 / KbQuotaHitRateHigh 临时调低限流）+ 8 条 promtool 合成单测（生产态难安全触发者，`alert-selfcheck/alert-rules-selfcheck-test.yml`，容器内 promtool 执行；第 8 条 KbPrimaryModelDegraded 为批4 新增，counter 跳变序列 + 外推收敛 2）。执行步骤与回传要求见 `docs/project-progress/用户侧待执行项清单.md` M1。

### 13.7 Grafana 资产与本地核验形态（v2.32 新增，Phase 4 簇② 任务 4.3）

**资产清单（infra/ 下，随仓库版本化）**：

| 资产 | 路径 | 要点 |
|---|---|---|
| Prometheus 主配置 | `infra/prometheus/prometheus.yml` | `rule_files` 接线 13.6 告警；scrape kb-rag-agent `/actuator/prometheus`（本地形态 `host.docker.internal:8090`，compose 已配 host-gateway 兼容 Linux；簇⑥ ECS 部署改服务名） |
| Grafana 数据源 | `infra/grafana/provisioning/datasources/datasource.yml` | **uid=prometheus 钉死**——四面板 JSON datasource 引用以此为准 |
| Grafana 面板 provider | `infra/grafana/provisioning/dashboards/dashboards.yml` | file provider 指向 `/var/lib/grafana/dashboards`（KB-RAG 文件夹） |
| 五面板 JSON | `infra/grafana/dashboards/kb-rag-{overview,retrieval,llm,business,supplier-sla}.json` | schemaVersion 39；面板构成见 13.4（supplier-sla 为 v2.58 批4 新增，provisioning 目录通配自动拾取） |
| 监控栈编排 | `infra/docker-compose.monitoring.yml` | Prometheus v2.55.1 + Grafana 11.5.2 + Jaeger all-in-one 1.62.0 + node_exporter v1.9.1（v2.56 批2 生产化：restart/healthcheck/限额/日志轮转/Grafana 口令 env 化，见 13.8） |
| 监控栈开机自启 | `infra/kb-monitoring.service` | systemd oneshot + RemainAfterExit（v2.56 批2，与 kb-api.service 同职责分层） |
| 告警合成单测 | `infra/prometheus/alert-selfcheck/alert-rules-selfcheck-test.yml` | 8 条难真触发规则的 promtool 单测（v2.56 批2 七条 + v2.58 批4 KbPrimaryModelDegraded，矩阵见 13.6 验证口径） |

**本地自核验形态（簇② 验收通道）**：`docker compose -f infra/docker-compose.monitoring.yml up -d`
起栈 → 宿主 `java --enable-preview -jar kb-api/target/*.jar`（或 spring-boot:run，注意
坑位：兄弟模块旧 jar / fork 参数偶丢，见 v2.32 ⑥）→ 造流量 → 核验四步：
① `/api/v1/targets` up；② 面板表达式对 `/api/v1/query` 逐条校验（无数据=EMPTY 合法、
语法错=400）；③ Grafana `/api/search` 四面板加载；④ 真实流量后面板曲线非空。

**Jaeger 验证通道（trace 类修复专用）**：`TRACING_OTLP_ENABLED=true` +
`LANGFUSE_OTLP_ENDPOINT=http://localhost:4318/v1/traces`（env 名沿用但指向本地
Jaeger，与 Langfuse 同 OTLP HTTP 全路径契约，无需 Langfuse 凭据）→ Jaeger UI 16686
查合树。簇② 经此通道裁决检索层传播修复（embedding 合树 ✓）；流式主生成 POST 残余
簇⑥ 批4 v2.58 源码级定案修复（根因与修复见顶部 v2.58 注记①），合树 E2E 裁决经本通道。

### 13.8 监控栈 ECS 生产化（v2.56 新增，Phase 4 簇⑥ 批2）

**生产化矩阵（docker-compose.monitoring.yml v2.56）**：

| 服务 | 镜像（钉版本） | restart | healthcheck | 限额（缺省） | 要点 |
|---|---|---|---|---|---|
| prometheus | prom/prometheus:v2.55.1 | unless-stopped | wget `/-/healthy`（busybox 基座自带） | 1G | retention 15d；自检单测目录挂 `/etc/prometheus/rules-selfcheck/` |
| grafana | grafana/grafana:11.5.2 | unless-stopped | wget `/api/health` | 512M | 口令经 infra/.env `GRAFANA_ADMIN_USER/PASSWORD` 注入，`:?` 守卫未设即拒起（明文 admin/admin 终结）；`depends_on: prometheus: service_healthy` |
| jaeger | jaegertracing/all-in-one:1.62.0 | unless-stopped | **不可行**（实证） | 256M | scratch 基座容器内无 shell/curl/wget——healthcheck 形态否决，自愈靠 restart + OTLP 导出侧容错（导出失败不阻断业务链） |
| node-exporter | prom/node-exporter:v1.9.1 | unless-stopped | —（无监听探针需求，轻量采集器） | 128M | host pid + /proc /sys / 只读挂载 + `--path.*` 重映射（官方 README 形态）；喂 kb-rag-host 告警组 |

**连接口径**：与应用面同为分层 compose 不共网络——Prometheus 经
`host.docker.internal:8090` 抓宿主映射端口（app compose 已暴露 8090），与批1b
「存量不纳管、宿主地址连接」一致；node-exporter 走监控 compose 内服务名
`node-exporter:9100`。将来共外部网络时 targets 可改服务名（prometheus.yml 注释留档）。

**告警自检矩阵（4.2 欠账清偿）**：14 条 = 6 真触发 + 8 合成单测（v2.58 批4 +KbPrimaryModelDegraded）。合成单测数值
构造纪律：counter 序列 2m 采样 + 奇数分钟求值点，使 `increase()` 窗口边界恰落
采样点之间、外推两侧对称，期望值精确收敛整数（本机无 promtool 环境，推导基准 =
extrapolatedRate 算法；ECS 首跑如有出入以 promtool 实测修订并回写）。四面板
+ 告警自检的完整 ECS 执行步骤统一收归 `docs/project-progress/用户侧待执行项清单.md`
M1（该清单为用户侧动作唯一源，2026-08-20 定案）。

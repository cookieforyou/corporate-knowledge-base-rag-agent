# 第十五章：测试策略

> 本章为《企业知识库 RAG Agent 工作台：Spring AI 2.0 全景实现报告》v2 拆分版的一部分（原第 六 卷「工程质量保障（质量层）」）
>
> [📑 返回目录](./README.md) · 最后更新：2026-08-13 · v2.28（簇⑥ D3）首次修订

> **v2.28（2026-08-13，簇⑥ D3）——Testcontainers 集成测试落地，15.2/15.3 草图形态定稿**：
> ① 宿主 **kb-eval**、failsafe `*IT.java`（`mvn verify`，CI 无 Docker 加 `-DskipITs`）；
> 容器单例基类 AbstractAdvisorChainIT：pgvector/pg17（init script 直接复用 kb-domain
> schema.sql 超级用户执行）+ redis-stack-server 7.4（**REDIS_ARGS 注密码；Redisson
> 视空串密码为有密码发 AUTH，容器必须 requirepass**）+ minio（生命周期 IT）；
> ES 不建容器——双写吞异常 + BM25 路超时降级空（纯向量单路形态，生产降级行为同测）。
> ② **模型桩**（15.3 定稿形态）：排除 SmartRoutingConfig（其 deepSeekChatModel 手工
> 装配校验真实 key）→ ItModelConfig 以可编程 StubChatModel 重建 smartRoutingChatModel
> （@Primary，真实路由包装器保留）；`spring.ai.model.embedding=none` 让位 +
> StubEmbeddingModel **hashing trick 词袋向量**（纯哈希桩查询≠文档相似度≈0 不可测检索，
> 共享 token 即相似 + IT 基线 similarity-threshold=0.1）；桩记录全请求消息列表作为
> grounding/记忆注入/拒答模板的断言通道；ChatClientAutoConfiguration 不可排除
> （QueryRoutingAdvisor 注入 ChatClient.Builder）。
> ③ **33 用例 × 11 IT**（2 context：DEFAULT + QUOTA @TestPropertySource 分组）：
> 租户隔离/跨租户泄露/Happy Path+空证据拒答/注入拦截+PII 掩码/输出黑名单（同步+流式）/
> 限流/Token 预算/多轮记忆/审计行完整性/意图路由 L1/L2/**文档生命周期**（簇⑥ C1
> 回归保险：reparse 确定性 ID 复现 + created_at 保留 + version 递增、replace 蓝绿
> diff 清理、软删）。Testcontainers 2.0.5（1.20.1 docker-java API 1.32 钉死与
> Docker Engine 29 最低 API 1.40 冲突，全请求 400）。
> ④ IT 挖出生产缺陷：PgVectorStore 默认 idType=UUID 与 kb_embeddings.id VARCHAR(36)
> 失配 → delete 静默失效，VectorStoreConfig 钉 idType(TEXT)（详见 7.2 v2.28）。
> 下方 15.2/15.3 为 v1 草图（Milvus/ES 容器 + 泛型 Mock 形态），已被上述实现取代。


## 15.1 测试金字塔

```
          ┌───────────┐
          │  E2E 测试  │  ← 关键业务流程（Playwright/Cypress）
          │  5-10 个  │
          ├───────────┤
          │ AI 评估    │  ← Golden Dataset 自动化评估（CI 集成）
          │ 200+ 用例  │
          ├───────────┤
          │ 集成测试   │  ← Testcontainers (PG+Milvus+ES+Redis)
          │ 50-80 个  │
          ├───────────┤
          │ 单元测试   │  ← JUnit5 + Mockito, 覆盖率 > 80%
          │ 200+ 个   │
          └───────────┘
```

## 15.2 Testcontainers 集成测试

```java
@SpringBootTest
@Testcontainers
class HybridDocumentRetrieverIntegrationTest {
    
    @Container
    static PostgreSQLContainer<?> pg = new PostgreSQLContainer<>("postgres:18");
    
    @Container
    static MilvusContainer milvus = new MilvusContainer("milvusdb/milvus:v2.6");
    
    @Container
    static ElasticsearchContainer es = new ElasticsearchContainer("elasticsearch:9.4.2");
    
    @Container
    static GenericContainer<?> redis = new GenericContainer<>("redis:8.0")
        .withExposedPorts(6379);
    
    @DynamicPropertySource
    static void configureProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", pg::getJdbcUrl);
        registry.add("kb.vector-store.provider", () -> "milvus");
        registry.add("kb.vector-store.milvus.host", milvus::getHost);
        registry.add("spring.elasticsearch.uris", 
            () -> "http://" + es.getHost() + ":" + es.getMappedPort(9200));
        registry.add("spring.data.redis.host", redis::getHost);
    }
    
    @Autowired
    private HybridDocumentRetriever hybridRetriever;
    
    @Test
    void shouldReturnFusedResultsWithBothSources() {
        // Given: 准备测试数据（文档经完整 ETL：向量化 + ES 双写）
        seedTestDocuments();
        
        // When: 执行混合检索（DocumentRetriever 语义，返回带溯源元数据的 Document）
        List<Document> results = hybridRetriever.retrieve(
            new Query("如何配置 Spring Boot 数据源？"));
        
        // Then: 融合结果携带双路得分元数据（10.1 元数据约定）
        assertThat(results).hasSizeGreaterThanOrEqualTo(1);
        assertThat(results.get(0).getMetadata().get("fusion_score"))
            .asInstanceOf(DOUBLE).isGreaterThan(0);
        assertThat(results).anyMatch(d -> d.getMetadata().get("vector_rank") != null);
        assertThat(results).anyMatch(d -> d.getMetadata().get("bm25_rank") != null);
    }
}
```

## 15.3 Mock LLM 测试策略

```java
@TestConfiguration
public class MockLlmConfig {
    @Bean
    @Primary
    public ChatModel mockChatModel() {
        return prompt -> {
            // 返回预设响应，避免调用真实 API
            String userMessage = prompt.getInstructions().get(0).getText();
            String mockAnswer = generateMockAnswer(userMessage);
            return new ChatResponse(List.of(
                new Generation(new AssistantMessage(mockAnswer))
            ));
        };
    }
}
```

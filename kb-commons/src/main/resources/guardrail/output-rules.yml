# 输出检测词表（安全簇① A1/A3，设计 12.7 词表工程）
#
# 词项模型同 injection-rules.yml；family 取输出三分类名（中性，OutputFamily）：
#   BUSINESS_CONFIDENTIAL（业务保密）/ COMPLIANCE_SENSITIVE（合规敏感）/
#   COMPETITOR_COMPARISON（竞品对比）——BLOCK 命中按分类选取安全话术，
#   替换计数按分类落子项指标 rag.guardrail.output.replaced.{分类}（T5）。
# value 同样逐条 Base64 编码态存储，加载层解码消费（第七节条 2）。
#
# 词项内容经 T4 带外通道注入（tools/guardrail/import_words.py，side=output）；
# T2 迁入的两条占位词（builtin-out-01/02，未解析占位符残留）已随 T5 同批退役，
# 退役记录见 tools/guardrail/import-manifest.json removals。
#
# 双源合并：本文件 ∪ rag.guardrail.output.blacklist（兼容并入）。
rules: []
